# Skill Harvest 報告 — 2026-07-26

## 一、Session 摘要

| # | 專案 | 訊息數 | 主要活動 |
|---|------|--------|----------|
| 1 | rivendell | 1 | Token 用量分析師 prompt：讀 2026-07-25 各專案 token 花費 + 當日 session 指令取樣，產出繁中日報 |
| 2 | rivendell | 1 | 同上，日期為 2026-07-24 |
| 3 | rivendell | 15 | 「新的 skills 都 push 了嗎」→ 確認並執行 git commit + push（13 次 Bash） |
| 4 | sales-assistant | 15 | 執行既有 `/crm-projection`：查 nx_client + nx_deal，比對 customer-intel，寫 materials/clients/ |
| 5 | sales-assistant | 15 | 同上，`/crm-projection` 再跑一次 |

已核對 `/Users/manibari/code/rivendell/skills/` 現有清單。

**Session 1、2 不算候選** — 查了 `bin/sk-token-analysis-cron`（launchd job `com.sk.agent.rivendell.token-analysis.plist`，每晚 23:45 跑），這兩則就是該 cron 產出的 prompt 本身，已經是全自動化基礎設施，不是手動重複的缺口。

**Session 4、5 不算候選** — `/crm-projection` 是既有 skill 的正常執行，重複跑兩次屬正常排程行為。

---

## 二、Skill 候選

### 🔴 Weak — 不建議新增：「skill 變更後 commit+push」

**目的**：使用者在 skills library 有新變更後問「都 push 了嗎」，AI 檢查 git 狀態、commit、push。

**依據**：Session 3，15 則訊息、13 次 Bash 呼叫，推測是 status → diff → add → commit → push 的標準序列。

**理由不建議**：
1. 這次 digest 只出現 1 次，還不構成「重複」模式的證據門檻。
2. 操作本身是標準 git 五步驟，沒有專案特定的判斷邏輯（不像 `repo-rename` 或 `concurrent-session-git` 那樣有踩坑知識要沉澱）。
3. 使用者全域 CLAUDE.md 已有「每完成一個功能/修復後，主動提醒我是否要 commit」的行為指令，這屬於**個人偏好**層級，不是流程缺口。
4. `.claude/CLAUDE.md` 的 Post-change rule 已經要求「skill 變更後同步 README」——如果之後多次觀察到「sync-readme → commit → push」這個**三步驟組合**重複出現，才有沉澱成 skill 的價值（做法是串接既有 `sync-readme`，不是重新造一個 git skill）。

---

## 三、本次無 Strong / Moderate 候選

這批 5 個 session 訊息量都很小（1 則或 15 則），且核心活動不是「已自動化」就是「既有 skill 正常執行」，沒有觀察到足夠訊噪比的新重複模式。

## 四、建議下一步

- 不新增 skill。
- 若「commit+push」模式在未來 harvest 中再出現 2 次以上，且開始牽涉 skills library 特定判斷（例如「哪些檔案該排除、commit message 該怎麼分組」），屆時再評估獨立 skill 或併入 `sync-readme`。
