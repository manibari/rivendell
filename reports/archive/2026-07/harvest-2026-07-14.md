# Session Harvest 報告 — 2026-07-14

> 來源:4 個 sessions(約 38 則訊息)的預先摘要 digest。樣本量偏小,且其中 1 個 session 無文字意圖、1 個 session 屬既有 skill 的執行維護,證據基礎有限——評級已反映此不確定性。

## Session 摘要

| # | 專案 | 訊息數 | 主要活動 | 工具分佈 |
|---|------|--------|----------|----------|
| 1 | PTI-ARES | 18 | 在 rulecheck 多版本頁面中定位 component spacing 的實作出處(考古式搜尋) | Bash×15, Read×1(page.tsx) |
| 2 | news-stock | 4 | 無文字意圖,僅 3 次 Bash——無可萃取內容 | Bash×3 |
| 3 | rivendell | 2 | 以「token 用量分析師」角色,把 2026-07-13 各專案 token 花費 + 使用者指令取樣寫成繁中日報 | Write×1(token-analysis-2026-07-13.md) |
| 4 | sales-assistant | 14 | 執行既有 `crm-projection` skill,並修改其腳本(crm_projection.py / crm_projection_run.py) | Bash×6, Read×2, Write×2, Skill×1, Edit×1 |

## 觀察到的模式

1. **重複多步驟 workflow**:僅 session 3 呈現可複製的完整流程(讀取花費資料 → 對照指令取樣 → 產出結構化繁中日報)。Session 1 是密集 Bash 搜尋,但屬一次性考古,尚未見第二次出現。
2. **領域知識**:session 3 隱含「token 花費 × 指令取樣 = 花費歸因」的分析框架;session 4 是 nx_client/nx_deal 資料投影,知識已封裝在既有 `crm-projection` skill 內。
3. **可編纂的問題解決模式**:session 4 顯示「headless 執行 skill → 發現腳本問題 → 現場修 skill 腳本」的維護迴圈,但這由 `skill-creator` / `self-improving-agent` 已涵蓋。

## Skill 候選

### 🟡 Moderate — `token-usage-report`

- **名稱**:token-usage-report
- **目的**:讀取每日各專案的 Claude Code token 花費,交叉比對該日 sessions 的使用者指令取樣,產出繁體中文日報(花費歸因 + 異常高耗 session 標記),寫入固定路徑(如 `reports/token-analysis-YYYY-MM-DD.md`)。
- **觸發**:`/token-usage-report`、「token 日報」、「用量分析」,或由 headless agent 每日排程觸發。
- **分類**:meta(rivendell 平台自我觀測)
- **理由**:Session 3 的 prompt 本身就是一份手寫的完整 skill 規格(角色 + 輸入格式 + 輸出格式),且日報性質天然會每日重複——這是「prompt 已成形、只差封裝」的典型收割對象。既有 `agent-observability` 管的是 script agent 的 dashboard 可見性,`workflow-retro` 管的是每週 workflow 瓶頸,均不涵蓋 token 花費歸因。評 Moderate 而非 Strong,是因為目前只觀察到 1 次執行;若下次 harvest 再見到同型 prompt,應升級為 Strong 並立即建 skill。

### 🔴 Weak — `feature-version-locator`(不建議建立)

- **名稱**:feature-version-locator
- **目的**:在同一頁面的多個版本副本(如 rulecheck v1/v2/v3 頁面)中,定位某個 UI 特性(如 component spacing)實際存在於哪個版本。
- **觸發**:「這個功能在哪個版本的頁面」「X 應該在某個版本裡」。
- **分類**:quality
- **理由**:Session 1 用了 15 次 Bash 做這件事,顯示搜尋成本不低;但這是通用 grep/glob 考古,Explore agent 與一般搜尋已能勝任,且僅出現 1 次、綁定 PTI-ARES 的特定版本目錄結構。除非同型需求跨專案再現,否則不值得固化。

### 無候選的 sessions

- **Session 2(news-stock)**:無文字意圖、僅 3 次 Bash,無可萃取模式。
- **Session 4(sales-assistant)**:全程在既有 `crm-projection` skill 的執行與維護範圍內,無新 skill 空間。另注意:sales-assistant 專案已標記 deprecated(遷移至 chimesflow 中),`crm-projection` / `customer-intel` / `material-health` 後續需隨遷移調整路徑,而非新增 skill。

## 建議行動

1. **觀察 `token-usage-report`**:若 rivendell 的 token 日報 prompt 再次出現(排程或手動),以 `skill-creator` 封裝,直接沿用 session 3 的 prompt 作為 SKILL.md 主體。
2. 不為 session 1 / 2 / 4 建立新 skill。
