# Session Harvest — 2026-07-01

> Skill 收割報告：檢視近期 session，抽出可複用的 skill 候選。
> 資料來源為預先摘要的 digest（4 sessions / ~210 msgs / cost 顯示 $0.00，可能未計費或本地執行）。
> 事實查核聲明：digest intent 欄位被截斷、tool 統計為主要證據；下述評級基於 tool/file 訊跡推論，未逐條讀原始 transcript。

## Session Summary

| # | 專案 | 訊息數 | 主要活動 | 關鍵檔案 |
|---|------|--------|----------|----------|
| 1 | PTI-ARES | 42 | 元件庫優化：seed 元件分類 + 關聯 rule | `component_category.py`, `component_lib_seed.py`, `db.py`, `ir_ingest.py`, `lib.py` |
| 2 | PTI-ARES | 71 | 幾何 canvas 互動：可點選 line/surface/pad，截圖驗證 | `GeometryCanvas.tsx`, `b_now.png`, `b_review.png`, `b_selected.png`, `b_top.png` |
| 3 | PTI-ARES | 80 | 渲染保真度：對齊設計/文件與 renderer | `canvas-renderer.ts`, `rendering-fidelity.md`, `schema.py`, `CHANGELOG.md`, `LEARNINGS.md` |
| 4 | sales-assistant | 17 | 執行既有 `crm-projection` skill（nx_client/nx_deal → markdown） | `INDEX.md`, `_crm_projection_gen.py`, `clients.py`, `projection.md` |

**Top tools**: Bash(83), Read(35), Edit(22), TaskCreate/Update(20), Write(7), Agent(6)。三個 PTI-ARES session 佔 ~90% 活動，屬同一產品的前後端打磨；sales-assistant 只是跑既有 skill。

## 觀察與領域重疊

- PTI-ARES 是 **ODB++/DFM/EDA 製造端工具**，領域知識已由既有的 `odb-dfm-reference`（backend）承載（Job→Step→Layer、符號渲染、depanel/registration 陷阱）。這三個 session 是該產品**自身**的元件庫 seed、canvas 互動、渲染保真度打磨 —— 屬 project-specific，宜進 PTI-ARES 自己的 `.learnings/` 或擴充 `odb-dfm-reference`，不宜升為新的通用 rivendell skill。
- Session 4 的 `crm-projection` **已存在**（skills/workflow/crm-projection），只是被 headless/手動觸發執行，非候選。
- Session 2 的「寫完 canvas 一定截圖自檢」呼應全域 CLAUDE.md 既有規則（生成圖後一律 headless 截圖 Read 檢查），已被規則覆蓋，不需再造 skill。

## Skill 候選

### 🟡 Moderate — `canvas-render-fidelity`（渲染保真度回歸）
- **Purpose**: 針對 Canvas/SVG 互動視圖（hit-testing 點選 line/surface/pad、狀態高亮），用 headless Chrome 對「操作前/選中/檢視」各狀態截圖 → Read 比對，並把 renderer 與資料 `schema` 的偏差寫成 `rendering-fidelity.md` 追蹤。把 session 2+3 反覆出現的「改 renderer → 截多張狀態圖 → 人眼/AI 比對 → 記錄偏差」流程固化。
- **Trigger**: 「canvas 渲染不對 / 點選沒反應 / 對齊設計稿」、修改 `*-renderer.ts`/`GeometryCanvas` 類互動畫布時。
- **Category**: quality（或 frontend）
- **Rationale**: 出現在 2 個 session（71+80 msgs），是重複的多步流程。但**與既有 `qa-auto`/`gstack-browse`/`gstack-qa` 部分重疊**（都是 headless 截圖驗證）；差異點是「互動幾何畫布的 hit-testing + 多狀態 diff + schema 偏差文件」。建議先確認差異是否足以獨立成 skill，或只補一段到既有 QA skill —— 因此評 Moderate 而非 Strong。

### ⚪ Weak — `seed-taxonomy-library`（分類 + rule 關聯 seed）
- **Purpose**: 從 taxonomy 建立分類化的種子資料庫（category → 實體 → 交叉關聯的 rule），如 session 1 的 `component_category.py` + `component_lib_seed.py` + rule 關聯。
- **Trigger**: 「建元件庫 / 種子分類資料 / seed data」。
- **Category**: backend
- **Rationale**: 只在 **1 個 session** 出現，且高度綁定 PTI-ARES 的元件/DFM 領域（rule 關聯是該產品業務邏輯）。缺乏跨專案複現證據，屬 project-specific。建議記入 PTI-ARES `.learnings/` 而非造 skill。

## 結論

- **Strong 候選：無。** 本批 session 集中在單一產品（PTI-ARES）的內部打磨，領域已被 `odb-dfm-reference` 覆蓋。
- **可行動項**：
  1. 若 canvas 互動渲染的截圖驗證跨產品再現一次 → 升 `canvas-render-fidelity` 為正式候選（目前 Moderate，先觀察）。
  2. Session 1/3 的偏差與 seed 慣例 → 寫進 PTI-ARES `.learnings/LEARNINGS.md`（platform/project vault），非 rivendell skill。
  3. 無需為 session 4 動作 —— `crm-projection` 已存在且正常運作。
