# Session Harvest — 2026-07-13

> 來源：3 個近期 session 的預先摘要 digest（~41 msgs，2 專案）。
> 已比對現有 skills（`skills/` 7 大分類 + `~/.claude/skills/`）與 `bin/sk-*` 腳本，僅列出**尚不存在**的候選。
> 本日結論先講：**無 Strong / Moderate 候選**——三個 session 全是排程 agent 命中既有資產（token 日報 cron、material-health、crm-projection），codify 工作已完成，本日 harvest 的價值在**復發問題追蹤**：sales-assistant 遷移第四次點名（本日已完成 plist 盤點）、digest metadata 失效第三次點名，另有一個新的檔案下落待查項。

## Session 摘要

| # | 專案 | 訊息數 | 主要活動 |
|---|------|--------|----------|
| 1 | rivendell | 1 | Token 用量日報（2026-07-12）：「token 用量分析師」prompt 產出繁中日報 |
| 2 | sales-assistant | 23 | 排程執行 `/material-health`：掃 `materials/` frontmatter 缺漏、過期補助、過時公司資訊 → 產出 `materials/HEALTH_REPORT.md`、更新 INDEX.md、修 chimei-foods / example-tech 兩檔 |
| 3 | sales-assistant | 17 | 排程執行 `/crm-projection`：查 nx_client + deal pipeline → 交叉比對 customer-intel → 更新 INDEX.md、projection.md；digest 另列 `crm_projection.py`、`feedback_auto_commit.md` 兩檔（下落待查，見觀察 4） |

**跨 session 觀察**

1. **三個 session 全數命中既有資產，零手工補救**：session 1 = `bin/sk-token-analysis-cron`（本次已驗證 `bin/` 內 `sk-token-analysis-cron` + `sk-token-snapshot` 均在）；session 2 = `skills/workflow/material-health`；session 3 = `skills/workflow/crm-projection`。連續三份 harvest（07-11、07-12、本日）確認這幾條排程 workflow 已 codify 完畢，無新抽取需求。
2. **sales-assistant deprecated 但排程 agent 仍在其上跑（第四次復發）**：07-09、07-11、07-12、本日（×2 sessions）。本日完成 07-12 建議的 plist 盤點：`~/Library/LaunchAgents/` 下有 **4 個** `com.sk.agent.sales.*` plist——`crm-projection`、`material-health`、`subsidy-scraper`、`tender-scraper`（注意 label 是 `sales.` 不是 `sales-assistant.`，前幾份報告的搜尋關鍵字要修正）。遷移清單見下方建議 1。
3. **digest metadata 失效第三次復發**：「Skills invoked:」空白（但 session 2–3 各有 Skill(1) 呼叫）、Cost 全為 $0.00。`sk-harvest-summarize` 萃取層問題持續未修。
4. **新待查項——session 3 的兩個檔案在磁碟上找不到**：digest 列出 `crm_projection.py` 與 `feedback_auto_commit.md`，但 `find` 遍 `~/code/sales-assistant`、`~/.claude/skills/crm-projection/`（僅 SKILL.md + projection.md）均無此二檔。可能落在其他 repo（nexus？）、已被後續動作移除、或 digest 檔名萃取有誤（與觀察 3 同源）。在修 `sk-harvest-summarize` 時一併確認。

---

## Skill 候選

### （本日無候選）

- **token 日報**（session 1）：`bin/sk-token-analysis-cron` + `bin/sk-token-snapshot` 已覆蓋（本次驗證仍在），純排程管線屬 bin 領土，維持 07-11 / 07-12 結論。
- **material-health**（session 2）：已存在於 `skills/workflow/material-health`，不重複建。
- **crm-projection**（session 3）：已存在於 `skills/workflow/crm-projection`，不重複建。
- **feedback 自動 commit pattern**（session 3 的 `feedback_auto_commit.md` 暗示排程 agent 把回饋寫檔＋自動 commit）：檔案本體找不到、僅一次出現、且 `.claude/hooks/auto-stage-hook.sh` + `headless-agent` skill 已覆蓋大半，證據不足不抽。待觀察 4 釐清後若確為新 pattern 再評。

依「寧可留白不可捏造」原則，不硬湊 Weak 候選。

---

## 已覆蓋、不需新 skill

| Session | 覆蓋資產 | 驗證方式 |
|---------|----------|----------|
| 1 | `bin/sk-token-analysis-cron`, `bin/sk-token-snapshot` | `ls bin/ \| grep token` 確認存在 |
| 2 | `skills/workflow/material-health` | skills 目錄清單確認；`materials/HEALTH_REPORT.md` 產出在 |
| 3 | `skills/workflow/crm-projection` | skills 目錄清單確認 |

## 建議下一步

1. **sales-assistant 遷移動工（第四次復發，本日已備好料）**：4 個 plist 已盤點完成——`com.sk.agent.sales.{crm-projection, material-health, subsidy-scraper, tender-scraper}.plist`。遷移動作：per skill 確認新落點（chimesflow 或 rivendell 本體）→ 改 plist 的 WorkingDirectory / label → `launchctl bootout` 舊的、`bootstrap` 新的 → 更新 agents.conf。tender-scraper 前幾份 harvest 未點名過，是本次新發現的第 4 隻。參照 `skills/git/repo-rename` checklist 執行。
2. **`sk-harvest-summarize` 總體檢（第三次復發，範圍擴大）**：(a)「Skills invoked:」空白；(b) Cost $0.00；(c) 新增症狀——檔名清單疑似不準（觀察 4 的兩個幽靈檔案）。三個症狀同源於 metadata 萃取層，一次修。
3. **觀察 4 釐清**：確認 `crm_projection.py` / `feedback_auto_commit.md` 實際寫去哪（或根本沒寫成功）。若 crm-projection 排程 run 在嘗試自我改寫腳本，需要知道它改到哪裡去了。
