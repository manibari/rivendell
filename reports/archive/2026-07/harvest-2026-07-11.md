# Session Harvest — 2026-07-11

> 來源：4 個近期 session 的預先摘要 digest（~26 msgs，2 專案）。
> 已比對現有 skills（`skills/` 7 大分類）與 `bin/sk-*` 腳本，僅列出**尚不存在**的候選。
> 本日結論先講：**無 Strong 候選**——四個 session 全是排程 headless agent 執行既有資產，沒有出現「現場重新發明的 workflow」。寧可留白，不硬湊候選。

## Session 摘要

| # | 專案 | 訊息數 | 主要活動 |
|---|------|--------|----------|
| 1 | rivendell | 1 | Token 用量日報（2026-07-09）：以「token 用量分析師」prompt 產出繁中 markdown 日報 |
| 2 | rivendell | 1 | Token 用量日報（2026-07-10）：同上，隔日排程再跑 |
| 3 | sales-assistant | 12 | 排程執行 `/crm-projection`：查 nx_client 活躍客戶 + deal pipeline → 交叉比對 customer-intel → 更新 `materials/clients/` 的 INDEX.md、projection.md |
| 4 | sales-assistant | 12 | 同 session 3——工具序列（Bash×7 / Read×2 / Skill×1）與檔案完全相同 |

**跨 session 觀察**

1. **全數為 headless 排程執行，且全數命中既有資產**：session 1–2 的 prompt 來自 `bin/sk-token-analysis-cron`（nightly 23:45，haiku 摘要），session 3–4 是 `skills/workflow/crm-projection` 的排程觸發。四個 session 都沒有 ad-hoc 補救或重複手工步驟——這是「可收割模式」的反面訊號：workflow 已經 codify 完了。
2. **Session 3 與 4 完全同構**：同 intent、同工具計數、同檔案。可能是兩日各跑一次（正常），也可能是重複排程或 digest 重複收錄。digest 未含時間戳，無法就地判定，列入下方待查。
3. **digest 的「Skills invoked:」欄位空白**，但 session 3–4 各有 Skill(1) 工具呼叫——`sk-harvest-summarize` 的 skill 名稱萃取疑似漏抓，影響 harvest 判斷品質（skill 命中率是「要不要抽新 skill」的主要證據）。列入待查。

---

## Skill 候選

### 🔴 Weak — token-daily-report（Token 用量日報）

- **目的**：每專案當日 token 花費 + session 意圖取樣 → 繁中日報。
- **不建議建 skill 的原因**：這條 workflow **已完整存在於 `bin/sk-token-analysis-cron`**（facts 由 dashboard `lib/tokens.py` 組裝、摘要走 `claude -p` + haiku、輸出 `reports/token-analysis-YYYY-MM-DD.md` + Telegram digest）。它是純排程管線、無互動觸發需求，屬 bin 腳本領土而非 skill 領土；建 skill 只會造出第二個 SoT。維持現狀。

### （無其他候選）

- **crm-projection**（session 3–4）已存在於 `skills/workflow/crm-projection`，排程執行正常，不重複建。
- Digest 中無互動 session、無新 domain 知識展開（無新 API pattern / 資料格式 / 除錯手法），無可 codify 的問題解決模式。

---

## 已覆蓋、不需新 skill

- **Session 1–2**：`bin/sk-token-analysis-cron` + `bin/sk-token-snapshot` 覆蓋，另有 dashboard tokens 頁（02bae8a commit：30 天視圖 + SQLite 持久化）承接「how much」面。
- **Session 3–4**：`crm-projection` skill 覆蓋。⚠️ **維運註記（第二次復發）**：sales-assistant 已標記 deprecated（遷移中 → chimesflow），但 crm-projection 排程 agent 仍在該 repo 執行。07-09 harvest 已對 `subsidy-scraper` 提出同一問題——現在確認 sales-assistant 上**至少兩個**排程 agent（subsidy-scraper、crm-projection）尚未列入遷移清單。依 memory，`crm-projection` / `customer-intel` / `material-health` 皆受遷移影響。

## 建議下一步

1. **sales-assistant 排程 agent 遷移清單**（優先，二次復發）：盤點該 repo 的 agents.conf / launchd plists（`com.sk.agent.sales-assistant.*`），把 crm-projection、subsidy-scraper、material-health 的新落點（chimesflow？）寫進遷移計畫。repo rename/遷移會斷 plist label 派生，參照 `skills/git/repo-rename` 的 checklist。
2. **查 `sk-harvest-summarize` 的 Skills invoked 萃取**：session 3–4 有 Skill 工具呼叫但 digest 頂部 "Skills invoked:" 為空，修復後 harvest 才能可靠判斷 skill 命中率。
3. **確認 session 3–4 是否重複收錄**：比對兩個 session JSONL 的時間戳；若同日雙跑，檢查 crm-projection 的排程是否重複註冊。
