# Session Harvest — 2026-07-30

**結論先行**：這 3 個 session 沒有夠格新增的 skill。其中 2 個是既有 skill（`subsidy-scraper`、`crm-projection`）的排程執行，只是在驗證它們還能跑；第 3 個是一次性簡報內容工作，落在既有 skill（`slide-template-extractor` / `chart-design` / `mermaid-diagram` / `iot-factory-report`）涵蓋範圍內，只是這次沒被觸發。比起造新 skill，更值得處理的是兩個維運訊號：一是 `subsidy-scraper`/`crm-projection` 仍指向已標記為廢棄的 sales-assistant 專案；二是 session 1 這類「延伸既有簡報」的任務沒有自動路由到對應 skill。

## Session 摘要

| # | 專案 | 時間 | 訊息數 | 主要活動 |
|---|------|------|--------|----------|
| 1 | `~/Vault/Peter/Work`（數位孿生與工安管理簡報） | 2026-07-30 00:28–00:53 UTC | 16 | 基於既有 pptx 往下寫兩個章節：系統架構（分層模組：資料串接／模型／3D 模型介接）＋落地應用；用 Bash 產圖／HTML 截圖驗證、Read 既有頁面截圖、AskUserQuestion 確認方向 |
| 2 | `~/code/sales-assistant`（subsidy-scraper） | 2026-07-29～30（排程 agent） | 42 | 執行 `/subsidy-scraper`：爬 grants.nat.gov.tw / SBIR / SIIR，對既有 `nx_*` 補助檔案去重，寫入／歸檔新補助案 markdown |
| 3 | `~/code/sales-assistant`（crm-projection） | 2026-07-29～30（排程 agent） | 17 | 執行 `/crm-projection`：query `nx_client` 所有 active 客戶 + deal pipeline，比對 customer-intel 報告，投影到本地 markdown |

Session 2、3 都是既有 skill 的正常排程執行（各自 `Skill(1)` 呼叫紀錄），不是新模式；session 1 沒有任何 Skill 呼叫。

## Skill 候選評估

沒有 Strong 或 Moderate 候選。列出評估過但排除的項目，附排除理由：

### Weak（不建議新增）— 簡報延伸內容產生器

- **可能命名**：`deck-content-continuation`
- **用途**：讀取既有 pptx／截圖，抓出視覺風格與既有章節，往下產生新章節（如系統架構圖、落地應用）並沿用同一視覺語言
- **觸發情境**：使用者說「基於這份簡報往下寫」「幫我加系統架構那頁」
- **分類**：docs / slide
- **排除理由**：這個工作流已經被三個既有 skill 分工覆蓋——`slide-template-extractor`（從參考 PPTX 抽風格）負責「鎖風格」、`mermaid-diagram`／`chart-design`／`iot-factory-report`（含 UPW／SCADA／模組化架構圖案例）負責「畫分層架構圖」、`gstack-design-review` 負責視覺一致性驗收。真正的缺口不是沒有 skill，是 session 1 沒有路由進這些 skill（本身走了 ad hoc Bash+Read+截圖）。與其造新 skill，不如檢查 `slide-template-extractor` 和 `iot-factory-report` 的 trigger 描述有沒有涵蓋「基於既有簡報往下寫」這種措辭。

## 順帶發現（非 skill 候選，但值得記錄）

1. **`subsidy-scraper` / `crm-projection` 仍依賴已廢棄專案**：依照既有記憶（sales-assistant-deprecated），`~/code/sales-assistant` 已於 2026-06-13 確認為 dead project，功能正在遷往 chimesflow。這兩個 skill 目前還在對 sales-assistant 的 `nx_client`／`nx_deal` 跑，代表遷移完成前每次排程執行都是對著逐漸沒人維護的資料源工作——這是既有已知風險，此次 harvest 只是再次確認它仍在運作中，沒有新資訊。
2. **Session 1 未觸發任何 skill**：使用者的措辭「基於這份簡報往下寫」沒有命中任何 skill 的 TRIGGER 清單（`slide-template-extractor`／`iot-factory-report`／`chart-design` 都沒有這類自然語句）。如果這種「延伸既有簡報」的請求會重複出現，下次可以在 `session-harvest` 或 `skill-creator` 觸發時特別看這個路由缺口是否累積到第 2 次。
