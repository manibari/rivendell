# Skill Harvest 報告 — 2026-07-27

## 一、Session 摘要

| # | 專案 | 訊息數 | 主要活動 |
|---|------|--------|----------|
| 1 | Vault-Peter-Work | 34 | `/office-pptx`：食品業智慧製造 AI 產品解決方案簡報（新建 pptx + slide 截圖比對） |
| 2 | Vault-Peter-Work | 69 | `/office-pptx`：把「115年營運處巡迴簡報」PDF 轉成可編輯 PPTX，自建 `build_overlay.py` 疊加可編輯文字框，反覆截縮圖比對驗證 |
| 3 | Vault-Peter-Work | 88 | 建立新商機「弘如企業（金屬加工）」公司檔案，3 次 AskUserQuestion 確認方向 |
| 4 | Vault-Peter-Work | 102 | 「上旺科技」104 職缺情蒐 → storyline 草擬 → `/de-slopify` 文字打磨 |
| 5 | Urd-BI | 65 | 除錯「工作台看不到東西」（`/workspace?picks=...` 空白），用 Chrome DevTools MCP 檢查 console + DOM |
| 6 | rivendell | 1 | Token 用量分析師 prompt：讀 2026-07-26 各專案 token 花費 + 當日指令取樣，產出繁中日報 |
| 7 | sales-assistant | 18 | 執行既有 `/material-health`：健檢 materials/ 是否有缺 frontmatter / 過期補助 / 過時公司資訊 |

已核對 `/Users/manibari/code/rivendell/skills/` 現有清單（11 大類、約 110 個 skill）。

**Session 6 不算候選** — 查了 `bin/sk-token-analysis-cron`，這是既有 launchd 排程（每晚跑），session 6 就是該 cron 產出的 prompt 本身，已是全自動化基礎設施，不是手動重複缺口。

**Session 3、4 不算候選** — 「新商機建檔」與「104 情蒐 + storyline + 文字打磨」的整條路徑已由既有 `presales-pipeline`（新客戶資料夾 scaffold）+ `customer-intel`（公司調查）+ `tw-company-lookup` + `slide-office-hours`（storyline 紅隊審查）+ `de-slopify` 覆蓋，這次只是這些既有 skill 的正常串接使用。

**Session 5、7 不算候選** — Session 5 的「Chrome DevTools 排查前端空白畫面」屬於 `gstack-investigate` / `gstack-browse` 既有涵蓋範圍（一次性 bug fix，非新流程）；Session 7 是既有 `material-health` skill 的排程執行。

---

## 二、Skill 候選

### 🟡 Moderate — 補強 `office-pptx`：「PDF → 可編輯 PPTX」轉換技法

**名稱**：（非新 skill，建議作為 `office-pptx` 的技法補充 / reference doc）PDF-to-editable-PPTX overlay 流程

**目的**：客戶或內部常拿到「已定稿、無法直接編輯」的簡報（掃描版 PDF、或原始 pptx 遺失只剩 PDF），需要還原成可編輯 PPTX 才能修改內容再送出。

**觸發情境**：使用者說「把這個 PDF 轉成可編輯的 pptx」、「這份簡報只有 PDF 檔幫我改」。

**類別**：`docs`（office-pptx 內部技法，非獨立 skill）

**理由**：
1. Session 2（69 則訊息、32 次 Bash、6 次 Edit）明確走了一條 `office-pptx` 目前 SKILL.md 沒寫的路線：先把每頁 PDF 轉成背景圖片，再自建 `build_overlay.py` 疊上與原文字位置/ 字型對齊的可編輯文字框，接著反覆輸出縮圖（`chk*.jpg` → `editable-thumbs.jpg` → `final-thumbs.jpg`）跟原稿比對，直到視覺一致才收尾。這個「背景圖 + 文字框疊圖 + 縮圖比對驗證」的迴圈，跟 CLAUDE.md「生成圖後一律自我截圖檢查」的既有紀律同構，值得沉澱成可重複的腳本/步驟，而不是每次重新手刻 `build_overlay.py`。
2. `office-pptx` SKILL.md 目前只寫了「PPTX → PDF → JPEG」方向（第 449 行起，用途是校驗），**沒有反向**「PDF → 背景圖 + 疊字 → PPTX」的教學或現成腳本。這是同一個 skill 領域內的真實缺口，不是全新領域。
3. 依專案規則（rivendell `.claude/CLAUDE.md`：先確認 skill 領域，不重複造）——這應該是**擴充 `office-pptx`**（新增一份 reference doc + 把 `build_overlay.py` 收進 `scripts/`），而非另開一個新 skill。
4. 只出現 1 次（本次 digest 範圍內），未達「重複 2 次以上」的沉澱門檻；若下次 harvest 再遇到「PDF 轉可編輯 PPTX」需求，建議直接動手補進 `office-pptx`，不用再等第三次。

---

## 三、本次無 Strong 候選

其餘活動（新商機建檔、B2B 情蒐+storyline+文字打磨、前端空白畫面除錯、token 日報、素材健檢）都落在既有 skill 的正常使用範圍內，或是已自動化的排程，沒有觀察到未被覆蓋的重複流程。

## 四、建議下一步

- 不新增獨立 skill。
- 待 `office-pptx` 有空檔時，把 session 2 的 `build_overlay.py` 技法（PDF 逐頁轉背景圖 → 疊可編輯文字框 → 縮圖比對）補進 `docs/office-pptx/` 作為 reference doc + 收錄腳本，避免下次同類需求又重新手刻。
- 若「PDF → 可編輯 PPTX」需求在未來 harvest 中再出現，直接視為補強觸發點，不用再累積案例。
