# Skill Harvest 報告 — 2026-08-12（跨專案：3 個 project，3 個 session）

**結論先說：本輪沒有 Strong/Moderate 候選。** 3 個 session 中，2 個是既有排程/skill
的正常執行（token 用量日報 agent、`crm-projection`），只有 1 個 session（iot-factory-report
延伸出互動式 BI 介面 + 大量瀏覽器來回驗證）帶有可能重複出現的新模式，但是 n=1，先記錄
不立案。

## 一、Session 摘要

| # | 專案 | 訊息數 | 主要活動 |
|---|------|-------|---------|
| 1 | Vault-Peter-Work | 318 | 從 `iot-factory-report` skill 起手（桃園佳世達設備資料），但需求擴大成「給我一個 BI 介面」，改成打造互動式 React 儀表板（`DashboardViews.tsx`、`ProjectDashboard.tsx`、`catalog.ts`），並用 `claude-in-chrome`（50 次 computer + 16 次 navigate）反覆截圖驗證畫面 |
| 2 | rivendell | 1 | Token 用量分析日報生成——已對應到既有 `agents/registry/token-analysis.md` 排程 agent，屬正常執行，非新模式 |
| 3 | sales-assistant | 15 | 正常執行既有 `crm-projection` skill：查 `nx_client`／`nx_deal` pipeline，比對 customer-intel 報告，寫出 `projection.md` |

已核對 `/Users/manibari/code/rivendell/skills/`：`crm-projection`、`iot-factory-report`
均已存在且對應正確；`agents/registry/token-analysis.md` 確認 session 2 是既有排程
agent 的例行輸出，不是新流程。

## 二、Skill 候選

### 🔴 Weak（記錄觀察，不立案）

- **Session 1（IoT 靜態報告 → 互動式 BI 儀表板 + 瀏覽器迭代驗證迴圈）**：使用者從
  `iot-factory-report`（設計目的是 CSV/Excel → HTML 圖表 + PPTX 靜態報告）出發，
  但實際要的是「BI 介面」——一個可操作的 React 儀表板頁面，於是工作內容變成
  改 `.tsx` 元件、用 `claude-in-chrome` 開瀏覽器截圖檢查、改一次看一次的迭代迴圈
  （Edit 28 次、computer 50 次、navigate 16 次）。這個「用瀏覽器自動化做 edit→
  screenshot→edit 收斂迴圈」的形狀，跟現有 `gstack-qa`（QA 抓 bug）、`run`
  （啟動+截圖確認）不完全同義——這裡是**開發中即時視覺驗證**，不是完工後測試。
  但目前只有這一次事件，且高度綁在這個專案的 dashboard-next 元件上，無法判斷
  下次類似需求會不會走同一條路。**Hold**：若下次又出現「skill 產出靜態報告，
  使用者臨場要求做成互動介面」這種 scope 擴張，或再出現一次「改元件→截圖驗證」
  的緊密迴圈，再考慮是否要在 `iot-factory-report` 裡加一個「進階模式：從報告
  升級成互動儀表板」的分支，或独立成一個通用的「邊改邊拿瀏覽器截圖驗證」的
  workflow skill。
- **Session 2（token 用量日報）**：已是既有 `agents/registry/token-analysis.md`
  排程 agent 的正常輸出，非新模式。
- **Session 3（crm-projection）**：既有 skill 正常排程呼叫，行為與定義一致。

## 三、建議下一步

1. 無新 skill 需求，本輪不建立任何新 skill。
2. Session 1 的「報告 skill 臨場升級成互動介面」模式：下次遇到類似「產出靜態
   報告的 skill，使用者當場要求做成可互動的 BI/dashboard」時，回頭比對這次的
   做法（改 React 元件 + claude-in-chrome 截圖迴圈），若形狀一致就考慮在
   `iot-factory-report` 加互動模式分支，或抽成獨立的「開發中即時視覺驗證」
   workflow skill。

**Trigger to revisit**：`iot-factory-report`（或其他報告類 skill）第 2 次被要求
升級成互動介面時再立案。
