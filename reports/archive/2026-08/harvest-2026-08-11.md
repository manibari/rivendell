# Skill Harvest 報告 — 2026-08-11（跨專案：6 個 project，7 個 session）

**結論先說：本輪沒有 Strong/Moderate 候選。** 7 個 session 分散在 6 個不同專案，
彼此之間形狀不重疊；多數是單一產品的功能開發或既有 skill 的正常呼叫，只有 2 個
session 帶有可能重複出現的資料處理模式，但都是 n=1，先記錄、不立案。

## 一、Session 摘要

| # | 專案 | 訊息數 | 主要活動 |
|---|------|-------|---------|
| 1 | Vault-Peter-Work | 40 | 光泉_中華電信AI 案 ETL：整合靜態資料 + 動態資料，補上天氣外部資料。產出 `etl_dynamic.py`／`etl_external.py`／`etl_merge.py` + `README.md`／`STATE.md` |
| 2 | ChimesFlow | 126 | 銷售/週會報告功能迭代：負責人後加「銷售產品」欄位、報告選擇加記憶性、加「預期金額」。改 `opportunities.py/ts`、`opportunity.py`、`page.tsx` |
| 3 | ChimesFlow | 113 | 設計相關工作，過程中切換到 Opus 5(1M) 並呼叫 6 次 Skill（推測含 design-consultation / user-flow 類）。產出 `design-system.md`、`findings.md`、flow 截圖 |
| 4 | Marketing-Pal | 41 | 執行某個 bundled skill（`/private/tmp/claude-501/bundled-skills/...`），搭配 `claude-in-chrome` 操作瀏覽器。意圖不明確，證據不足以判斷是否為新模式 |
| 5 | PTI-ARES | 41 | DFM 規則庫爭議：使用者質疑「沒定義規則卻有違規」，要求清空規則、依自己的方式重建。改 `library.py`／`projects.py`／`ruleset_export.py` |
| 6 | Verdandi-AutoML | 48 | 處理多個背景 task-notification（3 個 task-id），用 `TaskStop` 收掉部分任務；改 `auth.py`，新建 `dev-ports.md` |
| 7 | sales-assistant | 14 | 正常執行既有 `crm-projection` skill：查 `nx_client`／`nx_deal`，比對 customer-intel，寫出 `projection.md` |

已核對 `/Users/manibari/code/rivendell/skills/`：`crm-projection`、`odb-dfm-reference`
均已存在且對應正確；session 3 的設計流程對應到 `chimesflow-design`／`user-flow`／
`gstack-design-consultation` 現有覆蓋範圍。

## 二、Skill 候選

### 🟡 Weak（記錄觀察，不立案）

- **Session 1（多來源 ETL 合併：靜態＋動態＋天氣外部資料）**：`etl_dynamic.py` /
  `etl_external.py` / `etl_merge.py` 三檔分工清楚（靜態參照資料、動態事件/感測
  資料、外部情境資料如天氣，最後合併成分析用資料集），形狀比隨手寫的腳本更接近
  一個可重用的 ETL 骨架。但這是**光泉_中華電信AI 單一客戶案的第一次**，資料來源
  組合（靜態/動態/天氣）目前只服務這個 domain，不確定下一個案子的外部資料會是
  天氣還是別的（匯率、原物料價格等）。與既有 `iot-factory-report`（SCADA 時序→
  PPTX）層次不同——那個是「report 生成」，這個是「report 生成前的資料整併」，
  沒有現成 skill 覆蓋。**Hold**：等第 2 個案子出現同款「靜態+動態+外部資料合併」
  需求，再抽出通用 ETL merge 骨架（可能落在 backend/ 或跟 iot-factory-report 併）。
- **Session 6（背景任務叢發 + 手動 TaskStop 清理）**：同一個 session 內收到 3 個
  `task-notification`，並用 `TaskStop` 主動收掉部分任務，同時新建 `dev-ports.md`
  記錄 port 配置——這對應到 `~/.claude/CLAUDE.md` 已存在的 Engineering Gotcha
  「Port verification before 'X is running'」，該規則已經是文字化的原則，這次是
  該原則在 Verdandi-AutoML 專案內第一次落地成實體檔案。單一 session、單一專案，
  且行為本質是「照既有規則做事」而非發現新模式，不足以立案為 skill。
- **Session 2、3（ChimesFlow 功能迭代 + 設計流程）**：都是既有產品的正常增量開發，
  session 3 更明確呼叫了既有 design 類 skill，屬於覆蓋範圍內的正常使用。
- **Session 4（Marketing-Pal）**：資訊量太薄（intent 只有 bundled-skill 的系統
  路徑，無法判斷實際任務內容），無法判斷是否有可抽取模式。
- **Session 5（PTI-ARES 規則庫爭議）**：與先前 harvest（2026-08-10）記錄的 DFM
  規則庫矩陣 UI 屬同一產品同一區塊的延續性爭議（使用者質疑規則定義方式並要求
  重建），仍是單一產品內部的資料模型調整，非跨情境可重用流程。
- **Session 7（crm-projection）**：既有 skill 正常排程呼叫，行為與定義一致。

## 三、建議下一步

1. 無新 skill 需求，本輪不建立任何新 skill。
2. Session 1 的 ETL 合併模式：下次遇到「靜態＋動態＋外部資料來源合併」的客戶案時，
   回頭比對這次的三檔案分工，若形狀一致就抽成骨架（可能命名 `etl-merge-pipeline`
   或併入既有 domain skill）。
3. Session 6 的 dev-ports.md：目前僅是一次性文件，不需要動作；若未來有第二個
   專案也手動建立類似的 port registry，再考慮是否該有一個跨專案共用的 port
   registry 慣例（可能寫進 CLAUDE.md 而非另立 skill，比照現有 Engineering
   Gotchas 的處理方式）。

**Trigger to revisit**：ETL 合併遇到第 2 個客戶案；port registry 在第 2 個專案
重複出現手動建立的需求。
