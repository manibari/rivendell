# Skill Harvest 報告 — 2026-08-10（跨專案：PTI-ARES / sales-assistant）

**結論先說：本輪沒有 Strong/Moderate 候選。** 5 個 session 中 3 個是同一場
PTI-ARES 除錯／功能調整的連續片段（含一次 `/model` 切換造成的斷點），其餘 2 個
是既有 skill（`crm-projection`、`subsidy-scraper`）的正常排程呼叫，全落在現有
覆蓋範圍內。

## 一、Session 摘要

| # | 專案 | 訊息數 | 主要活動 |
|---|------|-------|---------|
| 1 | PTI-ARES | 267 | 大量 Bash/Edit + `claude-in-chrome` 截圖迭代，修 `GeometryCanvas.tsx`、更新 `CHANGELOG.md` 與 `MEMORY.md`；`/model` 切換到 Fable 5 造成 session 中斷 |
| 2 | PTI-ARES | 74 | 延續上一場（同一批截圖 `Screenshot 2026-08-04`/`2026-08-10`），改到 `canvas-renderer.ts`、`pack-decode.ts`，同樣以瀏覽器截圖驗證渲染結果 |
| 3 | PTI-ARES | 139 | 使用者糾正資料模型：cp0030 不該獨立成「成對間距」元件類別，應歸類為「元件 x 元件」；並要求比照 cp0010 在頁面下方加可調參數矩陣。改動 `library.py`、`rule_library_seed.py`、`page.tsx`、`pyproject.toml` |
| 4 | sales-assistant | 15 | 正常執行既有 `crm-projection` skill：查 `nx_client` 活躍客戶 + `nx_deal` pipeline，比對 customer-intel，寫出 `projection.md` |
| 5 | sales-assistant | 29 | 正常執行既有 `subsidy-scraper` skill：爬 grants.nat.gov.tw / SBIR / SIIR，去重後更新 `INDEX.md` |

工具分佈：Bash(286)、Edit(70)、`claude-in-chrome__computer`(46)、Read(33)、
TaskUpdate(15)、ToolSearch(10)、TaskCreate(9)、`claude-in-chrome__navigate`(9)、
SendMessage(3)、WebFetch(3)。本輪 5 個 session 皆未呼叫 `Skill` 工具本身
（session 4、5 的「Run the X skill」是外部 headless agent 直接指定 skill 目錄
執行，非互動中臨時觸發）。

已核對 `/Users/manibari/code/rivendell/skills/`（現有清單）：`crm-projection`、
`subsidy-scraper`、`odb-dfm-reference` 均已存在且對應正確。

## 二、Skill 候選

### 🔴 Weak — 不建議新增

- **Session 1+2（PTI-ARES 幾何渲染除錯）**：`GeometryCanvas.tsx` /
  `canvas-renderer.ts` / `pack-decode.ts` 的「改程式碼 → `claude-in-chrome` 截圖
  → 肉眼核對 → 再改」迴圈，本質是 CLAUDE.md 已記錄的「生成圖後一律自我截圖檢查」
  原則的程式碼版本，且兩個 session 是同一個除錯任務因 `/model` 切換被截斷的
  延續，不是兩個獨立情境各自重新發現同一模式。與 `gstack-browse`（headless
  截圖 QA）功能重疊，delta 不足以立案。
- **Session 3（DFM 規則庫矩陣 UI）**：cp0030/cp0010 是 PTI-ARES 特定產品頁面
  的資料模型與 UI 調整（元件類別歸類 + 參數矩陣呈現），屬單一產品的功能開發，
  非跨情境可重用流程。與 `odb-dfm-reference`（ODB++ 解析/DFM 規則參考）主題
  相關但層次不同——後者是「怎麼讀 ODB++」，這次是「規則庫頁面怎麼呈現」，
  n=1，暫不獨立成 skill。
- **Session 4、5（crm-projection / subsidy-scraper）**：都是既有 skill 依排程
  正常呼叫，行為與 skill 定義一致，非新模式。

## 三、建議下一步

1. 無新 skill 需求。
2. 若 PTI-ARES 幾何渲染除錯（Session 1+2）之後再出現第二次、且是不同 bug/情境
   下同樣的「截圖迭代除錯」需求，可考慮併入 `gstack-browse` 或
   `gstack-qa-only` 作為一個 mode，而非另開新 skill。
3. 若 cp0030/cp0010 這類「規則庫矩陣可調參數 UI」在 PTI-ARES 內再出現一次
   （例如下一個 cp00xx 規則導入時重複同樣的資料模型爭論），值得回頭檢視
   `odb-dfm-reference` 是否該補一節「規則庫 UI 呈現慣例」，避免每次都重新
   讓使用者糾正一次資料模型。
