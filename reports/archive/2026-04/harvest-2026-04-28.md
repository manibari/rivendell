---
date: 2026-04-28
sessions: 6
messages: 1118
projects: [Peter, news-stock, rivendell, sales-assistant]
---

# Session Harvest — 2026-04-28

## Session Summary

| # | 專案 | 訊息 | 主題 | 主要工具 |
|---|------|------|------|----------|
| 1 | Peter | 228 | PCB layout 會議記錄（SST/SSB 文字層、SMT pin1、DIP 貫穿孔、outline 圖層、連版/連晶） | Bash(74), Edit(43), Read(38) |
| 2 | news-stock | 94 | 將 6 大指標 + 後續查詢功能拆分為 Phase 1 / Phase 2 路線圖文件 | Bash(43), Edit(17), Write(10) |
| 3 | news-stock | 659 | 從月度財務報表反推 6 大指標（營收成長性 / 營業利益率 / 稅後淨利年增率 / 累積 EPS / 存貨周轉率 / FCF）的計算公式與 Vue 顯示元件 | Bash(332), Edit(96), Write(47) |
| 4 | rivendell | 91 | dashboard 掛掉後自動偵測 + 重啟機制（HTTP probe + watchdog），同步更新 README | Bash(55), Read(12), Edit(8) |
| 5 | sales-assistant | 10 | 執行既有 `crm-projection` skill | Skill(1), Bash(5) |
| 6 | sales-assistant | 36 | 政府標案搜尋（農委會 AI 相關）→ 在客戶卡新增 deal → 取案件原網址 | Bash(19), WebSearch(2), WebFetch(2) |

---

## Skill Candidates

### 1. service-watchdog-launchd（Strong）

- **Purpose**：為 macOS 上的 HTTP 服務（FastAPI / Next.js / dashboard）建立「HTTP probe + 自動重啟」watchdog，當服務卡住（process 還在但 HTTP 不回應）時自動 kill + restart。
- **Trigger**：使用者說「服務又掛了」「自動重啟」「watchdog」「HTTP healthcheck」「launchd 重啟」「dashboard 卡住」。
- **Category**：`workflow/`（搭配既有 `launchd-agent`）
- **Rationale**：
  - Session 4 (rivendell) 完整實作了這個模式：HTTP 探測 → 判斷 stuck → 觸發 restart，並寫入 `.watchdog-state` 紀錄。Recent commits（`2d6a27d`, `d836a94`）顯示這是新建立、剛驗證過的可重用模式。
  - 既有 `launchd-agent` 只教如何建立 plist；不涵蓋「process alive 但 HTTP 卡死」這個 macOS 上常見的失敗模式。
  - 高再用價值：任何長跑的 dev server 都會用到。
- **驗證**：`grep -r "watchdog" /Users/manibari/Documents/Projects/rivendell/skills/` → 無對應 skill。

### 2. financial-indicators-from-statements（Moderate）

- **Purpose**：從原始月/季財報資料（MOPS 三表）反推「6 大基本面指標」（營收成長性、營業利益率、稅後淨利 YoY、累積 EPS、存貨周轉率、FCF）的計算規則與 SQL/Python 實作。
- **Trigger**：使用者說「6 大指標」「基本面評分」「fundamentals grading」「自己算 ROE/EPS」「從月報算年化指標」。
- **Category**：`backend/`（鄰接 `mops-financial-scraper`）
- **Rationale**：
  - Session 3 投入 659 訊息、`fundamentals_grading.py` 為核心產物；公式邏輯（如「累積 EPS = 過去 4 季合計」「FCF = 營業現金流 − CapEx」）是高度可重用的領域知識。
  - 既有 `mops-financial-scraper` 只負責抓 + 落地原始三表；不涵蓋指標計算層。`investment-research` 是策略層，也不在這個層級。
  - 中度推薦：強依賴 news-stock 專案 schema，要設計成獨立 skill 需抽象化資料表結構，否則只是把該專案的程式碼搬出來。
- **建議**：若要做，定位成 `mops-financial-scraper` 的姊妹 skill，明文要求對方 schema 一致。

### 3. tender-lead-router（Moderate）

- **Purpose**：政府標案 → 銷售線索的轉換工作流：搜尋符合特定客戶能力的標案 → 取得原始公告網址 → 在 `nx_deal` / 客戶資料夾新增 deal/saleing 條目。
- **Trigger**：使用者說「幫我查 X 部會的標案」「在 Y 公司加一個 deal」「這案的網址給我」「標案配對客戶」。
- **Category**：`workflow/`
- **Rationale**：
  - Session 6 顯示這是固定流程：`tender-scraper` 結果 → 過濾關鍵字 → 人工挑選 → 寫入特定客戶的 saleing 段落 → 抓原始 g0v / 採購網連結。
  - 既有：`tender-scraper`（只抓）+ `crm-projection`（只投影 nx_client/nx_deal）+ `sales-material`（產 PPTX）。**中間「配對 + 寫入 deal」這段沒有 skill**。
  - 中度推薦：流程明確但只觀察到 1 個 session，再看 1–2 次重複出現再決定是否抽出。

### 4. roadmap-phasing（Weak）

- **Purpose**：把進行中的功能清單拆成 Phase 1 / Phase 2 的路線圖文件。
- **Trigger**：使用者說「Phase 1 / Phase 2」「路線圖」「分階段交付」。
- **Category**：`workflow/`
- **Rationale**：Session 2 的核心活動，但已被 `writing-plans` + `requirement` + `client-kickoff-docs` 覆蓋；不需要獨立 skill。

### 5. pcb-meeting-notes（Weak）

- **Purpose**：PCB layout 領域的會議記錄結構化（SST/SSB、pin1、outline、連版/連晶等專用詞彙）。
- **Trigger**：「PCB 會議記錄」「layout 會議」。
- **Category**：N/A
- **Rationale**：Session 1 雖長達 228 訊息，但屬於高度產業特化（PCBA 製造），重複出現機率低；**不建議**做成 skill。需要的是專案內的詞彙表，不是 reusable skill。

---

## Recommendations

1. **立即動手**：`service-watchdog-launchd`——剛在 rivendell 驗證過，趁記憶清楚把模式抽出。
2. **觀察一輪**：`tender-lead-router`、`financial-indicators-from-statements`——再看到 1–2 次類似流程再正式建 skill；現階段先在現有 skill 的 README 補一節說明銜接方式。
3. **不做**：`roadmap-phasing`、`pcb-meeting-notes`。

---

## 統計

- 已涵蓋 skills：`crm-projection`（Session 5 直接呼叫）。
- 新候選 skills：3 個（1 Strong, 2 Moderate）。
- 跳過：2 個（覆蓋重複 / 過度特化）。
