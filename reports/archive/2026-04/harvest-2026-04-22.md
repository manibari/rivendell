---
date: 2026-04-22
sessions_analyzed: 5
total_messages: 314
projects:
  - company-proflie
  - news-stock
  - sales-assistant
  - Documents/Projects
  - Documents (peter/chimesai/01_presales)
---

# Session Harvest Report — 2026-04-22

## Session Summary

| # | 專案 | 訊息 | 主要活動 |
|---|------|------|----------|
| 1 | company-proflie | 178 | 建立 MOPS 公開資訊觀測站爬蟲 → 財報/月營收/歷史營收落地 → 建 `finance_db.py` 作為 TEJ-like 資料庫後台；流程上先走 `requirement` + `gstack-office-hours` 定義需求 |
| 2 | news-stock | 43 | Investment research Continuous Mode — 讀 `portfolio-state.json` + WebSearch → 產出 `daily-2026-04-21.md`（完全走現有 `investment-research` skill） |
| 3 | sales-assistant | 17 | 呼叫 `crm-projection` skill：查 `nx_client` / `nx_deal` → 更新 `materials/clients/` 索引 |
| 4 | Documents/Projects | 35 | 啟動 sales-assistant 前後端，處理 `docker-compose.yml`、`auth.py`、port 對齊（`feedback_server_ports.md`）|
| 5 | Documents (presales) | 41 | `peter/chimesai/01_presales` 下新 client（宸祿科技）研究 → 寫 `client-readme.md` / `company-overview.md` → 以 active / archive / lost 分類 |

頂層工具：Bash(112), Edit(52), Write(24), TaskUpdate(23), Read(20), AskUserQuestion(18), TaskCreate(11), WebSearch(8), Skill(7), Grep(2)。

---

## Skill Candidates

### Strong

#### 1. `mops-financial-scraper` — 台灣公開資訊觀測站財報爬蟲 / TEJ-like 資料庫
- **Purpose**：自動化從 MOPS（`mopsov.twse.com.tw/mops/web/index`）抓取上市櫃公司的 (a) 歷史財務三表、(b) 月營收、(c) 季/年度合併營收，標準化落地到 SQLite/DuckDB（`finance_db.py` 模式），並提供類似 TEJ 的查詢 API，供後續量化分析使用。包含：股票代號清單、抓取節流、錯誤重試、增量更新、schema 版本控制。
- **Trigger**：使用者說「抓財報」「MOPS 爬蟲」「建一個像 TEJ 的資料庫」「月營收歷史」「公開資訊觀測站下載」「台股財務資料庫」。
- **Category**：`backend/`
- **Rationale**：Session 1 長達 178 訊息、Bash(43) + Edit(30) + Write(17)，實際產出 `collect_historical.py` / `finance_db.py` / `conftest.py`，是明確成形的工作流程。現有 `tw-company-lookup` 是抓 findbiz 登記資料、`customer-intel` 是 B2B sales research，都不是財務時序資料。對走量化 / news-stock / investment-research 的人等於補齊 Taiwan 資料源這塊拼圖。
- **是否存在**：否（`skills/backend/` 中無 MOPS 相關 skill，`tw-company-lookup` 僅涵蓋 findbiz）。

#### 2. `presales-pipeline` — 售前客戶管道資料夾管理
- **Purpose**：以檔案系統（`01_presales/<client-slug>/`）管理 B2B 售前 pipeline：`new-client.sh` 建立標準資料夾（`client-readme.md` + `company-overview.md` + 狀態 frontmatter），支援 `active` / `won` / `lost` / `archive` 狀態轉移，整合既有 `customer-intel` 做 one-shot research、以及 `crm-projection` 做 CRM 回填。產出 `INDEX.md` 彙總所有 prospect 現況。
- **Trigger**：使用者說「新 client」「presales」「這個可以 archive / lost 了」「宸祿科技」這類公司級 prospect 命名、或 `cd` 進入 `01_presales` 目錄時。
- **Category**：`workflow/`
- **Rationale**：Session 5 展現的工作模式跟 `customer-intel`（單次深度研究）、`crm-projection`（DB → markdown 投影）都不同——這是 prospect 的**生命週期**管理：新增 → 研究 → 判定 active/lost/archive → 歸檔。使用者已自建 `new-client.sh`，但分類語意（archive vs lost）與 INDEX 規則需要 codify。與 `material-health` 的「素材健康檢查」可進一步形成 pipeline 閉環。
- **是否存在**：否。`customer-intel`、`crm-projection`、`sales-material` 都處理片段，沒有 prospect folder lifecycle。

### Moderate

#### 3. `full-stack-devup` — 前後端一鍵啟動與 port 對齊
- **Purpose**：為多服務專案產生/維護啟動腳本：檢查 `docker-compose.yml` 服務、驗證與 `feedback_server_ports.md`（或 `.ports` / yesterday 的 `local-port-registry`）一致、跑健康檢查、前後端啟動後列印所有可點連結。失敗時自動診斷（port 被佔、auth.py import error 等）。
- **Trigger**：使用者說「啟動 X 前後端」「dev up」「本地跑起來」「port 對不上」。
- **Category**：`workflow/`
- **Rationale**：Session 4 用 Bash(27) 做的就是這件事——啟動 sales-assistant 的多服務堆疊，過程中要看 `AGENTS.md`、對 `docker-compose.yml` 與 `auth.py`、確認 `feedback_server_ports.md`。目前無對應 skill。與昨天提的 `local-port-registry` 可以綁定：port-registry 是資料、full-stack-devup 是 runtime。Moderate 而非 Strong 是因為每個專案的服務組合差異大，skill 要靠 convention-detection（偵測 `docker-compose.yml` / `Makefile` / `package.json` scripts）才通用。
- **是否存在**：否。`docker-compose-setup` 只負責**寫出** compose 檔，不負責**啟動與診斷**；`deploy` 是 production 流程不是 local dev。

### Weak / 不建議建立

- **Session 2（investment-research Continuous Mode）**：100% 走現有 `investment-research` skill，沒有新知識。
- **Session 3（crm-projection）**：直接呼叫既有 skill，無新 pattern。
- **Session 5 的公司研究本身**：已由 `customer-intel` + `tw-company-lookup` 涵蓋，不要重複建。`presales-pipeline` 只負責 folder lifecycle，**不要**把研究邏輯塞進去。

---

## 建議下一步

1. **優先建 `mops-financial-scraper`**（Strong、跨 news-stock / investment-research / company-profile 三個專案都受用，且 Session 1 已把主要程式碼原型寫出來，可直接封裝）。
2. **接著建 `presales-pipeline`**（Strong、補齊現有 sales skills 的 lifecycle 缺口）。動手前先把 Session 5 的 `new-client.sh` 讀進來當基底。
3. `full-stack-devup`（Moderate）可與昨天的 `local-port-registry` 合併為一個 skill，在實際第二次踩到 port 衝突時再做。
4. 不建議重做 investment-research / crm-projection / customer-intel 相關內容。
