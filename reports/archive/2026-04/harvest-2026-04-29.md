---
date: 2026-04-29
sessions: 7
messages: 575
projects: [Peter, "-----", news-stock, sales-assistant, sales-johnson]
---

# Session Harvest — 2026-04-29

## Session Summary

| # | 專案 | 訊息 | 主題 | 主要工具 |
|---|------|------|------|----------|
| 1 | Peter | 200 | 正傑科技官網（PCB Layout + AI Azure 雙主軸），含 CNAME / DESIGN.md / STATE.md / MEMORY.md 設計 | Bash(74), Edit(39), Write(36) |
| 2 | -----（地端公司辨識資料庫） | 219 | 設計地端 web app：PDF/Excel → 公司名稱辨識 → 既有公司去重；Claude SDK OAuth + Telegram 整合 | Edit(68), Bash(54), Write(43), TaskUpdate(15) |
| 3 | news-stock | 21 | investment-research Continuous Mode（讀 portfolio-state.json） | Bash(13), WebSearch(3), ToolSearch(1) |
| 4 | news-stock | 29 | investment-research Continuous Mode → 產出 daily-2026-04-28.md | WebSearch(14), Bash(10), Skill(1) |
| 5 | sales-assistant | 10 | 直接呼叫 crm-projection skill | Read(4), Bash(3), Skill(1) |
| 6 | sales-assistant | 21 | 重啟 frontend + backend dev server，先讀 AGENTS.md → 殺 stale → 啟動 → 驗證 port listening | Bash(17), Skill(1) |
| 7 | sales-johnson | 75 | clone GitHub repo（ChimesFlow）→ 在本地啟動 → 解 "failed to fetch" → port 3100/8100 對應 | Bash(61), Read(3), Edit(2) |

---

## Skill Candidates

### 1. repo-cold-start（Strong）

- **Purpose**：拿到一個沒看過的 GitHub repo，要在本地把它跑起來「看到畫面」。流程：clone → 偵測 stack（package.json / pyproject.toml / Dockerfile）→ 安裝依賴 → 補 `.env.local`/`.env` → 啟動前後端 → 驗證 port 是否 listening → 開瀏覽器確認，並針對常見「failed to fetch」（CORS / proxy / 前後端 port 對應錯誤 / API base URL）給出固定排查步驟。
- **Trigger**：使用者說「幫我跑這個 repo」「clone 這個然後讓我看畫面」「啟動讓我看畫面」「failed to fetch」「這個 repo 怎麼跑起來」、貼上 GitHub URL 後接「跑跑看」。
- **Category**：`workflow/`
- **Rationale**：
  - Session 7 投入 75 訊息、61 個 Bash 呼叫處理一個明確流程：fetch 失敗 → 給 port（3100/8100）→ 補 `.env.local` → 啟動 → 驗證。這正是「拿到別人的 repo」的固定痛點。
  - 既有 skill 有缺口：`init-project` 是建立**自己的**新專案；`env-doctor` 檢查環境**可重現性**；`gstack-investigate` 是 root cause 模式；`docker-compose-setup` 是出**新的** compose 檔。**沒有 skill 處理「外來 repo 第一次跑」**。
  - 高再用價值：跨 stack 通用（Next.js/FastAPI/Vite/Streamlit），且 "failed to fetch" 是固定 top-3 故障模式，可寫成 checklist。
- **驗證**：`ls /Users/manibari/Documents/Projects/rivendell/skills/workflow/` 無 `repo-cold-start` / `clone-and-run`；`backend/` 與 `meta/` 也無對應 skill。

### 2. dev-server-restart-verify（Moderate）

- **Purpose**：重啟 monorepo 的 frontend + backend dev server，並在回報前 **先驗證 port 真的在 listening**（避免「啟動完成但其實 crash」的假陽性）。流程：讀 AGENTS.md 找啟動指令 → 殺 stale process（lsof + kill）→ 在背景啟動 → `lsof -i :PORT` 或 `curl /health` 驗證 → 才回報「已啟動」。
- **Trigger**：使用者說「重啟 dev server」「frontend backend 都重開」「servers are down」「restart and verify」。
- **Category**：`workflow/`
- **Rationale**：
  - Session 6 有明確「先讀 AGENTS.md → 再驗證 ports listening before reporting」的 prompt，且這是 sales-assistant 反覆會遇到的狀態。
  - 與全域 CLAUDE.md 的「No completion claims without running verification commands first」原則一致——這個 skill 把「驗證」標準化。
  - 既有 `agent-observability` 是 dashboard log 整合；`launchd-agent` 是 macOS plist 管理；都不處理「reload + 立刻驗 port」這個短週期動作。
  - 中度而非強：只觀察到 1 次明確場景（Session 6），但模式很乾淨、CLAUDE.md 已經寫進原則，把 prompt 模板化的成本低。
- **建議**：實作時讓它讀 AGENTS.md 的 "How to run" 段落，自動推導 port 與啟動指令。

### 3. claude-sdk-oauth-telegram-bridge（Moderate）

- **Purpose**：把「Claude Agent SDK（OAuth 模式）」+ 「Telegram bot」+ 「地端 web app」三者組合起來：使用者透過 Telegram 上傳檔案 → 後端用 Claude OAuth token 呼叫模型做辨識 → 結果回 Telegram 並寫入地端 DB。涵蓋 OAuth scope、token 儲存（Fernet）、Telegram file_id → bytes、長任務的 ack 模式。
- **Trigger**：使用者說「Claude SDK + Telegram」「OAuth 串接 Telegram 辨識」「Telegram 上傳辨識公司名稱」「local 不上網但要用 Claude」。
- **Category**：`backend/`（鄰接 `oauth-token-vault` 與 `claude-to-telegram`）
- **Rationale**：
  - Session 2 的需求文件明確列出這個三層組合（"3. 用 claude sdk oauth 串接到 telegram 進行辨識"）。
  - 現有 `claude-to-telegram` 是「遠端控制 Claude Code session」(雙向 chat bridge)，不是「Telegram → Claude SDK → 自有 web app」。`oauth-token-vault` 與 `telegram-bot` 各自存在但**沒有 skill 把它們縫起來**。
  - 中度推薦：模式清楚但只看到 1 個專案有此需求，先觀察是否會在其他客戶/專案重複出現。

### 4. github-pages-corporate-site（Weak）

- **Purpose**：用 CNAME + DESIGN.md + STATE.md 模式建立企業官網（靜態 + 自訂域名 + GitHub Pages）。
- **Trigger**：「做官網」「靜態企業網站」「GitHub Pages 自訂域名」。
- **Category**：N/A
- **Rationale**：Session 1 雖大（200 訊息），但已被 `gstack-design-consultation` + `gstack-design-html` + `frontend-design` 覆蓋；CNAME / GH Pages 部署是 1 行設定，不需獨立 skill。

### 5. local-first-pdf-excel-company-extractor（Weak）

- **Purpose**：地端 PDF / Excel → 公司名稱抽取 + 既有公司去重的邏輯。
- **Trigger**：「PDF 抽公司名稱」「公司名稱去重」。
- **Category**：N/A
- **Rationale**：Session 2 的核心活動，但已被 `doc-to-structured-data`（PDF/Excel 解析）+ `tw-company-lookup`（統一編號比對）+ `markdown-file-ssot`（去重 SSOT）組合覆蓋；單獨抽出反而會與這三個 skill 重疊。

---

## Recommendations

1. **立即動手**：`repo-cold-start`——Session 7 完整跑過一輪、模式乾淨、跨 stack 通用，且既有 skill 真的有缺口。趁記憶清楚先寫。
2. **觀察一輪**：`dev-server-restart-verify`、`claude-sdk-oauth-telegram-bridge`——再看到 1–2 次再做。前者可以先在 sales-assistant 的 AGENTS.md 補一節 "Restart Protocol"；後者等 Session 2 專案實際開工再決定 skill 邊界。
3. **不做**：`github-pages-corporate-site`、`local-first-pdf-excel-company-extractor`——已被既有 skill 覆蓋。

---

## 統計

- 已涵蓋 skills：`investment-research`（Sessions 3–4）、`crm-projection`（Session 5）、`requirement`（Session 2 開頭引用）。
- 新候選 skills：3 個（1 Strong, 2 Moderate）。
- 跳過：2 個（覆蓋重複）。
- 觀察：Sessions 6 & 7 兩個獨立 session 都觸到「servers / port / 啟動驗證」議題，是這次 harvest 的主訊號。
