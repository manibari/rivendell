我先確認現有 skills 結構，3 個 sessions 信號量偏低，主要為現有 skill 的例行執行。

# Session Harvest Report — 2026-05-13

## Session 總覽

| # | 專案 | Messages | 主要工具 | 主要活動 |
|---|------|----------|---------|---------|
| 1 | news-stock | 38 | Edit(11), WebSearch(9), Bash(5), TodoWrite(5), Read(4) | 執行既有 `investment-research` skill 的 Continuous Mode — 讀 portfolio-state.json、寫 daily-2026-05-13.md |
| 2 | rivendell | 16 | Bash(15) | 純 Bash 操作，無 Intent 文字、無檔案產出 — 推測為 git/CLI 雜務（可能是 commit/rebase/檔案搬移） |
| 3 | sales-assistant | 11 | Bash(4), Read(3), Skill(1), Glob(1) | 執行既有 `crm-projection` skill — 輸出 INDEX.md / projection.md |

**綜合判斷**：3 個 sessions 中有 2 個是直接呼叫既有 skills（`investment-research`、`crm-projection`），第 3 個訊息量過少且無檔案產出。本次 harvest **新模式信號薄弱**。

---

## Skill 候選

### 1. portfolio-state-sync — **Weak**

- **Purpose**：投資組合每日狀態同步 — 讀 portfolio-state.json → WebSearch 各 ticker 最新消息 → 生成 daily-YYYY-MM-DD.md（含 alpha signals、risk flags、rebalance 提案）。
- **Trigger**：「更新投資組合」「daily portfolio sync」「持倉狀態更新」
- **Category**：`workflow/`
- **Rationale**：
  - Session 1 的 11 Edit + 9 WebSearch + 5 TodoWrite 顯示「逐 ticker 搜尋新聞 → 更新 daily report」的固定流程。
  - **但**：這個流程已經是 `investment-research` skill 的 Continuous Mode 一部分。從現有 skill 抽出獨立 sub-skill 的價值不足 — Session 1 沒有遇到任何痛點需要 codify。
  - **建議不獨立成 skill**，若 `investment-research` 的 Continuous Mode 模板需要強化，直接更新其內部 SKILL.md 即可。

### 2. bash-only-session-pattern（觀察項，非候選）— **Weak**

- Session 2 用 15 個 Bash 完成任務但 0 Edit / 0 Write，類似 Session 1 的 reports/ 目錄移動模式。
- 訊息量太少且無 Intent，**證據不足以判斷是否為可重複模式**，本次跳過。

---

## 建議行動

1. **本次無需新建 skill** — 3 個 sessions 中 2 個是既有 skill 例行執行（健康訊號），第 3 個訊息量過少。
2. **追蹤項**：未來 2–3 次 harvest 若再看到「daily portfolio 同步」遇到模板/流程痛點，再考慮從 `investment-research` 抽出 `portfolio-state-sync` 子節點。
3. **數據品質注意**：Session 2 缺 Intent + 0 Edit + 0 Write，建議檢查 harvest digest 工具是否正確抓取 user prompt（可能漏判 prompt token）。

低收成是正常結果 — 既有 skills 覆蓋率達 2/3，這比硬生造新 skill 更有價值。

---

是否要我接著檢查 harvest digest 工具，看 Session 2 的 Intent 為何顯示 `(no text)`？
