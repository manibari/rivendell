# Skill Harvest Report — 2026-05-23

## 摘要

| 項目 | 數值 |
|------|------|
| 分析 sessions 數 | 3 |
| 訊息總數 | ~39 |
| 涉及專案 | news_stock、sales-assistant |
| 主要工具 | WebSearch(15)、Bash(12)、Read(5)、ToolSearch(1)、Write(1)、Skill(1) |
| **新 skill 候選結論** | **本批次無 Strong/Moderate 候選；1 個 Weak（後續觀察）** |

---

## Session 拆解

### [1] news_stock：investment-research Continuous Mode（31 msgs，WebSearch×15 + Bash×10 + Read×3）

- **意圖**：以 `investment-research` Continuous Mode 接 `daily-2026-05-20.md` → 產出 `daily-2026-05-23.md` + 更新 `portfolio-state.json`
- **工具軌跡**：Read state → WebSearch（NVDA earnings / Samsung strike / Iran-US talks / Brent / 10Y / Philly Fed / CCJ / CEG-VST / FOMC minutes）→ Bash（grep / 整檔 sanity check）→ Write 新報告
- **產出**：`daily-2026-05-23.md`（週末 recap，8 連週 SPY ATH / Day 83 全現金紀律 vs 機會成本）、`portfolio-state.json`（watchlist 25 檔含 NVDA / CCJ / GEV / VST / CEG / 2330.TW 等的 stop / score / entry_condition 更新）、多筆 alerts
- **既有 skill 對映**：✅ `workflow/investment-research`（已存在）— **完全 cover**，包含 daily/weekly cadence、state 維護、stop-loss 評估、alpha 紀律檢討
- **觀察**：15 次 WebSearch 全集中在「事件後驗證」（earnings 實際數字、央行談話、地緣協商進度），符合 skill 既定 pattern — 無新增 workflow

### [2] sales-assistant：crm-projection（7 msgs，Read×2 + Bash×2 + Skill×1）

- **意圖**：跑 `crm-projection` skill，產出 `materials/clients/INDEX.md` + `projection.md`
- **工具軌跡**：Skill 觸發 → Read SKILL.md → Bash 查 `nx_client`/`nx_deal` → Write 兩份 markdown
- **既有 skill 對映**：✅ `workflow/crm-projection`（已存在）

### [3] sales-assistant：crm-projection 重跑（1 msg，0 tools）

- **意圖**：同 [2]，立刻第二次觸發
- **觀察**：明顯是 idempotent 重跑 / 排程觸發。0 tools = skill 內部判斷已 up-to-date 或直接交給 headless agent
- **既有 skill 對映**：同上

---

## 跨 session 模式分析

1. **WebSearch 高頻 (15 次) 全屬 investment-research 內建子流程** — 不是新候選 workflow，是現有 skill 的執行細節。
2. **crm-projection 連跑** — 與 [2026-05-21 harvest](harvest-2026-05-21.md) 觀察一致：本週連 2 次出現「同一天兩次觸發 crm-projection」。屬於 skill **運行特性**（idempotent / 排程）而非 **新 skill 訊號**。
3. **無新 domain 出現** — 3 個 sessions 全落在「投資研究 / CRM projection」兩條已 cover 的軌道內。
4. **無多步驟跨工具 workflow 浮現** — 沒有「A → B → C 一致序列」出現 ≥2 次的新 pattern。

---

## Skill 候選評估

### 🟡 Weak 候選（觀察用）：`crm-projection-scheduler-health`

| 屬性 | 內容 |
|------|------|
| **目的** | 監測 `crm-projection` 重跑頻率 / 輸出一致性 / idempotency，若同日連跑 ≥2 次自動 dedupe |
| **觸發** | headless agent post-run hook，或 `/material-health` 內嵌檢查 |
| **分類** | `meta/` 或併入 `workflow/material-health` |
| **理由** | 連 2 個 harvest 日（5/21、5/23）都觀察到 crm-projection 同日重跑 → 可能反映：排程 cron 設了兩次、上次未成功留下半成品、或是 user 主動 + agent 自動撞日 |
| **不推薦現在抽 skill 的原因** | 1) 樣本仍小（2 次觀察）；2) 解法可能是**改 crm-projection 自身的 idempotency**或排程設定，不是新 skill；3) 與既有 `material-health` 職責重疊 |
| **建議行動** | **不抽新 skill**。改為向 `crm-projection` 本身提 issue：(a) 加 mtime check「24h 內已跑過就 skip」(b) stderr.log 標明「skipped: already fresh」(c) 檢查 launchd / cron 是否誤設兩次。1 個月後若仍重複觀察，再評估 |

### ❌ 已評估、確認不抽
- **Investment research daily cadence** — `workflow/investment-research` Continuous Mode 已 cover。
- **WebSearch + state-update loop** — 是 `investment-research` 內部步驟，不是 cross-domain skill。
- **CRM projection workflow** — `workflow/crm-projection` 已 cover。

---

## 結論與建議

1. **本批次 3 個 sessions、2 個 domain，全部命中既有 skill** — 與 5/21 harvest 結論一致：skill library 對日常自動化覆蓋率高。
2. **無 Strong / Moderate 候選**。Weak 候選為觀察項，不執行 `/skill-creator`。
3. **可執行的 follow-up（非 skill 化）**：
   - 檢查 `~/Library/LaunchAgents/com.sk.agent.sales-assistant.crm-projection.plist` 是否設定多次 `StartCalendarInterval`
   - 在 `crm-projection` 加 idempotent guard（檢查 `materials/clients/INDEX.md` mtime）
   - 在下次 `/workflow-retro` 內提報「crm-projection 重跑」為候選優化項
4. **下次 harvest 重點**：等樣本累積到 ≥10 sessions 或出現新 domain（如非投資/銷售/補助）再跑。連續 2 個 harvest 都無 Strong 候選 → 提醒自己**不要為了交差硬擠**。

---

> 📌 路由結論：本報告**不建議呼叫 `/skill-creator`** — 無達 Strong / Moderate 門檻的候選。
> 建議 follow-up：檢查 crm-projection 排程與 idempotency（屬於 skill 內部優化，非新 skill）。
