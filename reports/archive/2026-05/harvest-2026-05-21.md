# Skill Harvest Report — 2026-05-21

## 摘要

| 項目 | 數值 |
|------|------|
| 分析 sessions 數 | 5 |
| 訊息總數 | ~52 |
| 涉及專案 | Peter-Work、news_stock、sales-assistant |
| 主要工具 | Bash(15)、WebSearch(11)、Read(9)、Edit(5) |
| **新 skill 候選結論** | **本批次無 Strong/Moderate 候選，1 個 Weak（觀察用）** |

---

## Session 拆解

### [1] Peter-Work：力成 HTML 啟動（6 msgs，Bash×5）
- **意圖**：在本機啟動力成（Powertech）的 HTML 報告畫面預覽
- **工具軌跡**：純 Bash（推測為 `python -m http.server` 或 `open <file>` 流程）
- **既有 skill 對映**：無直接對應；屬於一次性本機檔案操作

### [2] news_stock：investment-research Continuous Mode（27 msgs，WebSearch×11 + Edit×5 + Read×4）
- **意圖**：讀 `portfolio-state.json` + `daily-2026-05-20.md`，跑投資研究持續模式
- **既有 skill 對映**：✅ `investment-research`（已存在於 `workflow/`）— 完全 cover

### [3] sales-assistant：subsidy-scraper（1 msg，0 tools）
- **意圖**：跑 grants.nat.gov.tw / SBIR / SIIR 補助案爬取
- **既有 skill 對映**：✅ `subsidy-scraper`（已存在於 `workflow/`）

### [4][5] sales-assistant：crm-projection 兩次連跑（6 + 12 msgs）
- **意圖**：兩次呼叫同一個 `crm-projection` skill
- **工具軌跡**：Skill + Read + Bash + Glob → 產出 `INDEX.md` + `projection.md`
- **既有 skill 對映**：✅ `crm-projection`（已存在於 `workflow/`）
- **觀察**：第一次 6 msgs 似乎未完整輸出（只 1 個檔案），第二次 12 msgs 才出現 INDEX.md。可能是 idempotent 重試案例，但不足以結晶為新 skill。

---

## Skill 候選評估

### 🟡 Weak 候選：`html-report-local-preview`

| 屬性 | 內容 |
|------|------|
| **目的** | 一鍵在本機啟動 HTTP server + 開瀏覽器預覽客戶 HTML 報告（e.g. 力成、其他客戶 deck）|
| **觸發** | 「啟動 [客戶] 的 html 畫面」、「打開 HTML 報告本地預覽」|
| **分類** | `quality/` 或 `workflow/` |
| **理由** | Session [1] 完整流程是純 Bash 操作（找路徑 → 啟 server → 開瀏覽器），如果**未來反覆出現**可考慮抽 skill |
| **不推薦現在抽的原因** | 1) 樣本數=1，不滿足「2+ recurrences」門檻；2) 等同 `python -m http.server 8000 && open http://localhost:8000/...` 三行 alias，skill 化會 over-engineer；3) `gstack-browse` skill 已可 cover 「啟動瀏覽器 + 截圖驗證」場景 |
| **建議行動** | **不抽 skill**。若一個月內再次出現「啟動客戶 HTML」需求 ≥2 次，再回頭評估 |

---

## 結論與建議

1. **本批次 5 個 sessions 中，4 個直接命中既有 skill**（investment-research / subsidy-scraper / crm-projection×2）— 顯示 skill library 對日常工作流的覆蓋率已高。
2. **唯一未對映的 Session [1] 屬於一次性本機操作**，無 skill 化價值。
3. **建議改在 `crm-projection` 本身做 retro**：兩次連跑（Session 4 → 5）暗示第一次未完整輸出。可檢查 skill 是否需要：
   - idempotent 重跑（不重新覆蓋已成功的 client projection）
   - 中斷恢復（從上次成功的 client 接續）
   - 失敗時的明確 error 而非默默結束
4. **下次 harvest 重點**：等樣本累積到 ≥10 sessions 或出現新 domain（如非投資/銷售/補助的工作流）再跑，避免在低樣本下硬擠候選。

---

> 📌 路由結論：本報告**不建議呼叫 `/skill-creator`** — 沒有達到 Strong/Moderate 門檻的候選。
