我先確認現有 skills 後分析這 3 個 sessions。

# Session Harvest Report — 2026-05-12

## Session 總覽

| # | 日期 | 專案 | Messages | 主要活動 |
|---|------|------|----------|---------|
| 1 | 2026-05-12 | Peter-Work | 78 | 將立積電子簡報 PDF 轉成 HTML，準備 5/13 模擬報告（Q&A 演練） |
| 2 | 2026-05-12 | Peter-Work | 37 | 時序資料品質檢核（每秒一筆）→ 聚合每分鐘 → wide table 拆 dataset → cache 存檔 |
| 3 | 2026-05-12 | Peter | 10 | 訊息過少，意圖不明（僅有 STATE.md 互動） |

**工具使用集中度**：Bash(53) + Edit(24) + Read(13) + Write(5)，反映兩條主線：HTML/Markdown 編輯密集，與 CLI 資料處理密集。

---

## Skill 候選

### 1. presentation-qa-rehearsal — **Strong**

- **Purpose**：將既定簡報（PDF/PPTX/HTML）轉成「模擬問答演練表」— 預測評審/客戶會問的問題、生成模範答案、輸出 HTML 卡片式演練介面（含難度標記、回答時長提示）。
- **Trigger**：「模擬報告」「Q&A 演練」「答辯準備」「報告前演練」「rehearsal」「mock interview for presentation」
- **Category**：`docs/` 或 `workflow/`
- **Rationale**：
  - Session 1 顯示完整工作流：讀 PDF → 萃取重點 → 生成 Q&A 對 → 輸出 qa-rehearsal.html + qa-rehearsal.md，跨 24 Edit + 19 Bash 操作。
  - 現有 skills（`slide-workflow`、`pitch-deck`、`slide-office-hours`）都聚焦「生成簡報」或「storyline red team」，**沒有針對「報告當天前一晚的 Q&A 預演」這個情境**。
  - 立積案例顯示產業特定的串接邏輯（IC 設計 + 晶圓廠 + 封測廠）需要被結構化提問，這個 pattern 在補助案/投標/客戶提案前都會重演。
  - 與 `slide-office-hours`（storyline 階段）形成互補：office-hours 是「上場前一週的內容紅隊」，這個是「上場前一夜的口頭演練」。

### 2. timeseries-resampling-pipeline — **Moderate**

- **Purpose**：時序資料前處理 pipeline — 資料密度檢核（是否符合宣稱頻率）、降採樣聚合（1Hz → 1min）、寬表拆分（wide → 多個 long datasets）、本地 cache 持久化（pickle/parquet）。
- **Trigger**：「每秒一筆」「資料品質」「聚合每分鐘」「resample」「wide 拆 long」「downsample」「cache 存檔」
- **Category**：`backend/` 或 `workflow/`
- **Rationale**：
  - Session 2 顯示 28 個 Bash + 2 個 Read 的密集 pandas/CLI 操作，但沒有任何 Edit/Write — 表示是探索性資料工作，每次都重新摸索。
  - 現有 `iot-factory-report` 是「端到端報告產出」，**不包含獨立的資料前處理子流程**，且常用情境是「拿到客戶 raw CSV → 先確認品質與聚合策略 → 再決定要不要做完整報告」。
  - 抽出為獨立 skill 的價值：可被 `iot-factory-report`、`investment-research`、`mops-financial-scraper` 重複呼叫。
  - **降為 Moderate 而非 Strong** 的原因：只看到一個 session 的證據，且部分邏輯（resample、wide-to-long）是通用 pandas 慣用法，需要更多 session 確認是否真有可重複的「客戶 IoT 資料前處理」獨特模式（而非泛用資料工程）。

### 3. pdf-deck-to-html-rehearsal — **Weak**

- **Purpose**：PDF 簡報 → HTML 互動式演練版（含 speaker view、計時器、跳頁）。
- **Trigger**：「PDF 轉 HTML 簡報」「把 deck 變互動」
- **Rationale**：
  - Session 1 確實有此動作，但**已被現有 skills 涵蓋**：`office-pdf` 讀 PDF、`gstack-design-html` 產 HTML、`slide-workflow` 控簡報結構。
  - 「轉 HTML」本身不是新模式，**只有「為了模擬演練而轉 HTML」這個目的才是新的** — 那價值已歸入候選 #1。
  - **建議不獨立成 skill**，併入候選 #1 的實作細節即可。

---

## 建議行動

1. **優先**：執行 `/skill-creator` 起草 `presentation-qa-rehearsal`，並用 Session 1 的 `qa-rehearsal.html` / `qa-rehearsal.md` 作為範本。
2. **次要**：再觀察 1–2 個類似 session 後決定 `timeseries-resampling-pipeline` 是否獨立 — 或先在 `iot-factory-report` 內補一個 `data-prep` 子節點。
3. **跳過**：候選 #3。

Session 3 訊息量太少（10 msgs，無明確意圖），本次不貢獻任何候選。

---

是否要我接著起草 `presentation-qa-rehearsal` 的 SKILL.md？
