# Session Harvest Report — 2026-05-30

> 來源為 3 場 session 的預摘要 digest（非完整逐字）。已對照 `skills/` 全庫（7 類、~90 skills）做去重。

## 📊 Session 摘要

| # | 專案 | 訊息數 | 主要活動 | 關鍵工具 |
|---|------|--------|----------|----------|
| 1 | Vault/Peter-Work | 166 | AOI 視覺檢測 **PoC Phase 1 驗收項目**撰寫 + 簡報複查；分類別準確度門檻（銅箔 90% / 極性點 80%），「先用 POC 的猜測標」做暫時標註 | Edit(73), Bash(40), Read(14), Write(7) |
| 2 | sales-assistant | 11 | 執行 `crm-projection` skill（投影 nx_client/nx_deal、對照 customer-intel） | Read(4), Bash(4), Skill(1) |
| 3 | code | 29 | **fork 外部 GitHub repo**（Jacktsai2785/taiwan-company）→ refactor，產 task_plan/findings/progress | Bash(12), Agent(3), Write(3), AskUserQuestion(2) |

> **Session 2 直接呼叫既有 `crm-projection` skill**（與 2026-05-29 報告的 session 3 同一支，連兩日出現 = 穩定排程使用），無新 pattern，**略過**。

---

## 🎯 Skill 候選評級

### 1. `cv-poc-acceptance-criteria` — **Moderate** ⭐（watch list）

| 欄位 | 內容 |
|------|------|
| **Purpose** | 把電腦視覺/AOI 概念驗證的 Phase 1 驗收項目標準化：**逐缺陷類別準確度門檻**（如銅箔 90% / 極性點 80%，依難度與商業風險分層）→ **暫時標註策略**（ground truth 未齊時先用 POC 猜測標 + 標記信任度，後續回填）→ enricher 資料補強 → 驗收證據（accuracy 截圖、混淆矩陣）。 |
| **Trigger** | 「寫 PoC 驗收項目」「AOI/視覺檢測 phase 1 acceptance」「分缺陷類別訂準確度門檻」「先用猜測標」，或售前/交付情境要把 CV 模型成效寫成可簽核的驗收標準。 |
| **Category** | `workflow`（或 `quality`） |
| **去重** | `requirement` 為**通用** user story/acceptance criteria，不含 CV 領域的分類別門檻、暫時標註信任度、label 回填這類陷阱。`iot-factory-report` 處理感測時序，非影像缺陷。`qa-planner`/`qa-testing` 針對 web app 功能測試，不碰模型準確度驗收。**非冗餘。** |
| **Rationale** | 帶真實領域知識（風險分層門檻、provisional labeling、enricher 補強），通用 `requirement` 無法涵蓋。**但**僅單次出現、Edit(73) 顯示是同一份文件的反覆打磨而非穩定流程；且 `.claude/CLAUDE.md` 已將「視覺檢測報告 (AOI/SPC) ★ 暫無 skill」列為**已知 domain gap，待真實案子才抽**——本案正是此 case。**建議：第 2 個 CV/AOI 驗收案出現即升 Strong 並抽 skill。** |

### 2. `fork-and-onboard-repo` — **Weak**（建議不抽）

| 欄位 | 內容 |
|------|------|
| **Purpose** | 一鍵把外部 GitHub repo fork → 在地化 onboard：`gh fork` → 產 `CLAUDE.md`（init-project）→ `planning-with-files`（task_plan/findings/progress）→ 平行 Agent 探勘 → refactor → codex review。 |
| **Category** | `workflow` |
| **去重** | 機制已被四支既有 skill 共同覆蓋 ~90%：`init-project`（產 CLAUDE.md）、`planning-with-files`（三件套）、`github-repo-audit`（盤點）、`gstack-codex`（複查）。新 skill 只是把呼叫順序固定化。 |
| **不抽的理由** | (1) 單次出現、無重複證據；(2) 真正的價值在「fork→init→plan→review」的**順序編排**，但這恰好是 `~/.claude/CLAUDE.md` Backend/Refactor 流程已用文字 routing 涵蓋的，硬抽成 skill 反而與 routing 重複；(3) 各步驟差異大（每個 repo 的 refactor 內容不同），data-file 化收益低。沿用既有四支 + routing 即可。 |

---

## 🧠 萃取到的可複用知識（非 skill）

**Session 1 — CV/AOI 驗收領域（值得未來 skill 化的核心資產）：**
- 驗收門檻**分缺陷類別**而非單一總準確度：難度高/風險低者放寬（銅箔 90%），影響極性判讀者收緊或反之（極性點 80%）——門檻是商業風險與技術難度的函數。
- ground truth 未齊時的 **provisional labeling**：「先用 POC 的猜測標」+ 標信任度，避免阻塞，後續以人工複查回填。
- enricher.py 模式：原始影像/標註資料先過 enrich 再進驗收，與 2026-05-29 data-PoC 的「ETL/特徵 enrich」一脈相承。

**Session 2 — 確認 `crm-projection` 為穩定排程 skill**（連兩日 session 出現、純執行無修改），無需動作。

---

## ✅ 結論與行動

- **本次無 Strong 候選，不新增 skill。** 3 場中 1 場為既有 skill 執行、另 2 場皆單次出現。
- `cv-poc-acceptance-criteria`（Moderate）→ **watch list**，與 CLAUDE.md 既有 AOI/SPC domain-gap 追蹤合流，**第 2 案出現即抽**。
- `fork-and-onboard-repo`（Weak）→ **駁回**，既有四支 skill + CLAUDE.md routing 已覆蓋。
- 觀察到一條跨日訊號：`cv-poc-acceptance-criteria` 與昨日的 `data-poc-scoping` 都指向同一個更大的 gap——**「售前 PoC scoping/驗收」這個 domain branch 反覆出現**。若第三個 PoC 案再來，應考慮抽一支總管「PoC 規劃→驗收」的 skill，而非各領域各抽。
