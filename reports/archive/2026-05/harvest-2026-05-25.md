# Skill Harvest Report — 2026-05-25

## 摘要

| 項目 | 數值 |
|------|------|
| 分析 sessions 數 | 7（2 個為 0-tool 排程觸發空跑） |
| 訊息總數 | ~105 |
| 涉及專案 | Peter-Work（ChimesAI 工作目錄）、resume-pool、rivendell、sales-assistant（×4） |
| 主要工具 | Bash(43)、Read(14)、Agent(11)、Write(8)、Edit(6)、Skill(4)、Grep(2) |
| **新 skill 候選結論** | **無 Strong；2 Moderate（皆建議併入既有 skill / 改用模板）、其餘已被既有 skill 覆蓋** |

> 7 個 session 中有 **5 個是既有排程 agent skill 的執行**（workflow-retro、material-health、crm-projection ×2、subsidy-scraper）。只有 [1]、[2] 兩個含潛在新 pattern，且兩者都落在既有 skill 的領域邊界內。

---

## Session 拆解

### [1] Peter-Work：中華電新案子腳手架（7 msgs，Bash×4 / Write×2）
- **意圖**：「幫我在中華電建立新的案子，已成交，叫 202606_中華電信 AI 訓練課程」。
- **產出 / 軌跡**：在 `Work/中華電/02-Deliveries/` 下建立 `202606_中華電信 AI 訓練課程/`，內含：
  - `README.md`（`type: client-profile` frontmatter：channel / industry / relationship_status / current_phase / tags / created / updated）
  - `STATE.md`（當前階段 / 關鍵事實摘自 SOT / 風險待辦 / 下次 session 切入點）
  - 4 個 lifecycle 子資料夾 `1-presales / 2-contract / 3-delivery / 4-closure` + `cache/` + `workspace/`
- **既有 skill 對映**：
  - `client-kickoff-docs` → ⚠️ 相關但**不同**：它是「NDA 簽完讀 homework → scope/deadline/MEMORY/README 四件套」，本案是「已成交 → deal lifecycle 腳手架」。
  - `init-project` → ❌ 只建 CLAUDE.md/AGENTS.md（code 專案），不涵蓋商務 deal 結構。
  - `crm-projection` → ❌ 是 `nx_client` → markdown 投影（sales-assistant 系統），非 Peter-Work 目錄。
- **可複用訊號**：此 channel → lifecycle-stage → `YYYYMM_客戶名` 結構**已重複**（`202512_立積電子` 同樣有 README+STATE+lifecycle 子資料夾）。是一個確定性、可模板化的結構。

### [2] resume-pool：履歷判讀 + 可信度交叉詰問（42 msgs，Bash×12 / Agent×11 / Edit×6 / Read×3 / Skill×1）
- **意圖**：「downloads 有一系列履歷幫判讀」→ 升級為「**你能不能自己交叉詰問一下，誰看起來比較真實啊**」。
- **軌跡**：`candidate-analysis` skill 起手 → 用 **Agent×11 平行派工**對多份履歷做對抗式比對 → 沉澱出 `feedback_resume_credibility_lens.md` 記憶。
- **方法學（已寫入記憶）**：兩個硬性 lens——
  1. **時間 vs 專案內容物理上限**：日曆年資 ≠ 產出；扣掉 onboarding / ramp-up / handover 後的淨窗口能否塞下宣稱的交付（N 個跨域專案 / M 個月做時間除法）。
  2. **企業權限折扣**：大廠 mid-level IC 的決策權限有界，「主導 / 從 0 設計 / architected」需折扣；小公司（1~30 人）端到端 ownership 反而可信。看動詞克制度（Delivered/Implemented vs 自我加冕的 architected/主導）。
- **既有 skill 對映**：`candidate-analysis` 已存在，但其 workflow 聚焦**萃取 + GitHub 評分 + profile 產生**，**不含「跨履歷對抗式可信度詰問」這一步**。

### [3] rivendell：workflow-retro 執行（21 msgs，Bash×14 / Read×3 / Skill×1 / Write×1）
- **意圖 / 結論**：執行既有 `workflow-retro` skill，產出 `workflow-retro-2026-W22.md`（13.5 KB，完成）。`-error.log` 為 0-byte 殘留檔，retro 本身成功 → **非 bug、非新候選**。

### [4][7] sales-assistant：crm-projection / subsidy-scraper 空跑（各 1 msg，0 tools）
- 排程 agent 觸發但無實質工具呼叫（可能 early-exit / 無新資料）。**既有 skill，無新 pattern**。

### [5] sales-assistant：material-health 執行（12 msgs，Bash×6 / Write×2 / Skill×1 / Read×1）
- 執行既有 `material-health` skill，產出 `HEALTH_REPORT.md`。**已覆蓋，無新候選**。

### [6] sales-assistant：crm-projection 執行（21 msgs，Read×7 / Bash×7 / Grep×2 / Write×2 / Skill×1）
- 執行既有 `crm-projection`，動到 `_crm_projection_run.py` / `clients.py` / `crm-projection.sh` / `crm_proj.py` 等實作檔。屬 skill 內部實作維護，**非新候選**。

---

## 跨 session 模式分析

1. **排程 agent fleet 健康**：7 個 session 中 5 個是既有排程 skill（retro / material-health / crm-projection ×2 / subsidy-scraper）的正常執行 → 印證自動化覆蓋良好，本週無新自動化缺口浮現。
2. **無「A→B→C 一致序列 ≥2 次」的跨 session 新 workflow**。兩個含 pattern 的 session（[1]、[2]）彼此不相關，且各自落在既有 skill 的領域邊界內。
3. **Agent×11 用於對抗式判讀**（[2]）：「自我交叉詰問」用平行 agent 實作，是 `candidate-analysis` 可吸收的**機制**，而非獨立 workflow。
4. **結構化商務目錄腳手架**（[1]）是確定性模板任務，更適合用模板（Work/CLAUDE.md 既有 channel 規則）承載，而非重型 skill。

---

## Skill 候選評估

### 🟠 Moderate（建議併入）：`candidate-analysis` 增加「可信度交叉詰問」步驟
| 屬性 | 內容 |
|------|------|
| **目的** | 在現有萃取 + GitHub 評分後，新增一步：跨多份履歷做對抗式可信度詰問，套用兩個 lens（時間-vs-範疇物理上限、企業權限折扣），標出時間矛盾與大廠「主導/architect」浮報。 |
| **觸發** | 「誰比較真實」「交叉詰問」「judge credibility」「誰看起來在唬」（在多份履歷比較情境）。 |
| **類別** | workflow（hiring）。 |
| **理由 / 建議** | 方法學已 user-validated 並寫入 `feedback_resume_credibility_lens.md`，但這是**對 `candidate-analysis` 的能力擴充**，非新領域 → 不建議另開 skill。建議在 `candidate-analysis/SKILL.md` 新增 Step「可信度交叉詰問」，把兩個 lens 與「Agent 平行對抗式比對」機制固化進去，並標註每段任職的公司人數以校準宣稱。 |

### 🟠 Moderate（建議改用模板）：ChimesAI deal lifecycle 腳手架
| 屬性 | 內容 |
|------|------|
| **目的** | 在 `Work/<channel>/<lifecycle-stage>/` 下一鍵建立 `YYYYMM_<客戶名>/`：`README.md`（client-profile frontmatter）+ `STATE.md`（階段/事實/風險/下次切入點）+ `1-presales~4-closure` 子資料夾 + `cache/` + `workspace/`。 |
| **觸發** | 「建立新案子」「新增客戶 deal」「已成交建交付資料夾」（在 Peter-Work 目錄）。 |
| **類別** | workflow（business-ops）。 |
| **理由 / 建議** | 結構**確定且已重複**（立積電子、中華電信兩案同模板），但：(a) 該目錄已有自己的 `Work/CLAUDE.md` 承載 channel/lifecycle 規則；(b) 腳手架本身只是 mkdir + 兩個模板檔；(c) 僅 1 次明確 session 觸發。→ **建議先把 README/STATE 模板與目錄規範寫進 `Work/CLAUDE.md`**，若第 3 次重複出現再升級為 Strong 並抽 skill（比照 `client-kickoff-docs` 的「3rd occurrence 升級」慣例）。注意領域邊界：Peter-Work 屬個人商務工作區，非 rivendell 內部自動化 territory。 |

### 結論
- **本週無 Strong 候選。**
- 兩個 Moderate 都**不建議新開 skill**：[2] 併入 `candidate-analysis`、[1] 用 `Work/CLAUDE.md` 模板承載並觀察第 3 次出現。
- 其餘 5 個 session 為既有排程 skill 的正常執行，無缺口。

---

*Generated by session-harvest · 2026-05-25*
