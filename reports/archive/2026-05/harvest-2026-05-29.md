分析完成。兩個並行 agent 都讀了實際產出檔案並對照既有 skill 庫做去重。以下是收割報告。

---

# Session Harvest Report — 2026-05-29

## 📊 Session 摘要

| # | 專案 | 訊息數 | 主要活動 | 關鍵工具 |
|---|------|--------|----------|----------|
| 1 | Vault/Peter | 92 | 瑞典朋友九份一日遊**計劃書**（先建行事曆 → 中途改口要文件） | Bash(27), Edit(22), Read(11), Write(7) |
| 2 | Vault/Peter | 13 | 光泉 demand/waste forecasting **PoC 四段 scoping**（原始資料/ETL/模型/看板） | Bash(6), Read(3), AskUserQuestion(1) |
| 3 | sales-assistant | 16 | 執行 `crm-projection` skill（投影 nx_client/nx_deal） | Bash(7), Read(6), Skill(1) |

> Session 3 直接呼叫已存在的 `crm-projection` skill，無新 pattern，**略過**。

---

## 🎯 Skill 候選評級

### 1. `data-poc-scoping` — **Moderate** ⭐

| 欄位 | 內容 |
|------|------|
| **Purpose** | 把資料/ML 概念驗證（PoC）切成四段標準骨架：**原始資料**（schema 盤點 + 信任度標記 measured/derived）→ **ETL**（特徵 enrich、缺值/重複/anchor 對齊、label noise 分離）→ **模型建置**（naive baseline → panel LightGBM / Hierarchical TS → DeepAR/TFT，含報廢成本 vs 缺貨成本雙目標 loss）→ **看板建置**，配 Phase 0–3 工時估與 selling point。 |
| **Trigger** | 「寫一個 POC 小計劃」「針對 原始資料/ETL/模型/看板 分段規劃」「demand/waste forecasting 起手式」「資料拿到了要怎麼切 PoC」，或 presales 情境要把交付的時序/進銷存資料規劃成可簽單 PoC。 |
| **Category** | `workflow` |
| **去重** | `writing-plans`/`planning-with-files`/`requirement` 皆為**通用**規劃骨架，不含資料科學 PoC 的領域 checklist（信任度標記、label noise、時序就緒、雙目標 loss），**非冗餘**。`presales-pipeline` 只管 client 資料夾與狀態流轉，不碰 PoC 內容。`iot-factory-report` 處理 IoT/SCADA 感測時序，與零售進銷存 demand/waste 領域相鄰但不重疊。 |
| **Rationale** | 框架可複用且帶**真實領域知識**（報廢欄被簿存差額污染 → label noise 分離、月初 anchor reanchor、颱風拆成連續 exogenous 特徵、panel 時序就緒判定），這些是通用規劃 skill 無法涵蓋的陷阱。**但**僅單次出現（13 則），且 `.claude/CLAUDE.md` 已將「製造運營/時序製程分析」標為**已知 domain gap，待真實案子才抽 skill**——光泉正是此 case。**建議：再遇第 2 個類似 data-PoC 售前案（如奇美食品）即升 Strong 並抽 skill。** |

### 2. `personal-trip-itinerary-builder` — **Weak** ❌（建議不抽）

| 欄位 | 內容 |
|------|------|
| **Purpose** | 從一句行程描述生成一日遊計劃書：時間軸總覽表 → 逐站詳細 → 雨天/風險備案表 → 事前 checklist，輸出 HTML(A4)+MD+PDF 三件套。 |
| **Category** | `docs` |
| **去重** | `doc-coauthoring`（通用結構化長文/表格/checklist）+ `office-pdf`（HTML→PDF）已共同覆蓋本案 ~90% 機制。 |
| **不抽的四個理由** | (1) 個人 vault 行程，**外溢**於本 library 鎖定的 sales/automation 商業域；(2) 可複用機制與 `doc-coauthoring`+`office-pdf` 高度重疊，新 skill 只是換模板；(3) 本案最易出錯處是**台灣在地地理**（被 Peter 連續校正 4 次），那已寫成 memory rule `taiwan-local-geography`——是**一條規則不是 skill**，skill 化也救不了準確度；(4) 單次個人任務、無重複證據。calendar→document 的 pivot 顯示是臨時需求而非穩定 workflow。 |

---

## 🧠 萃取到的可複用知識（已落地，非 skill）

**Session 1 — 已寫入 memory `taiwan-local-geography`：**
- 台灣在地地理（餐廳位置、城市間車程、週末車流）AI 憑記憶推估誤差大，應先問本地人或標「需驗證」。
- Pattern：被使用者反問「你認真？」= 校正訊號，立刻信任在地經驗、停止用常識辯護。
- A4 列印 HTML 慣例：`@page size A4 margin 14mm`，11px 字級配單頁密度。

**Session 2 — 領域知識（值得未來 skill 化的核心資產）：**
- 進銷存等式 `期末=期初+進貨-銷售-報廢`；月內累加 + 月初 reanchor。
- **報廢不是獨立可預測對象**，是「生產決策 × 預測誤差」的損失函數；正確目標 `min[E(報廢成本)+E(缺貨成本)]`，核心預測對象是需求。
- 颱風為 exogenous，拆成 `days_to_landfall/intensity/distance_to_path`，不可當 binary holiday。
- Panel 時序就緒判定：6 店×8 SKU=48 條 daily×426 天 → cross-series ML 成立。

---

## ✅ 結論與行動

- **本次無 Strong 候選，不新增 skill。**
- `data-poc-scoping`（Moderate）放入 watch list——**第 2 個 data-PoC 售前案出現即抽**，與 CLAUDE.md 既有 domain-gap 追蹤一致。
- `personal-trip-itinerary-builder`（Weak）**駁回**，靠 `doc-coauthoring` + memory rule 即可。

---

兩點要請你決定：

1. **是否把這份 harvest 寫成檔案？** 依 rivendell 慣例會落在 `reports/harvest-2026-05-29.md`（git status 顯示該檔已存在/未追蹤）。但 `.claude/CLAUDE.md` 規定 `reports/*` 由排程 agent 擁有、互動 session 不應手動編輯提交——所以我**預設只在對話呈現**，要落檔再說。

2. **`data-poc-scoping` 要不要現在就記一筆 FEATURE_REQUEST / watch-list？** 我可寫進 `rivendell/.learnings/FEATURE_REQUESTS.md`（延續 2026-05-18 的 domain-gap 追蹤），標「第 2 案出現即抽」。這比放在報告裡更不會遺失。

要我執行哪一個（或兩個都做）？
