# Session Harvest Report — 2026-06-28

> Session-harvest 分析：4 個近期 session（~70 則訊息）。本批次**收割產出低**——多數 session 是在執行既有 skill，而非發現新的可復用模式。

## Session 摘要

| # | 日期 | 專案 | 主要活動 | 訊息數 | 工具 |
|---|------|------|----------|--------|------|
| 1 | 2026-06-28 | Vault/Peter-Family | 個人生活規劃：把「求婚週行動計畫」寫成 markdown（產檢 / 求婚 / 告知雙親 / 歐洲行程 / 戒指） | 23 | Edit(8), Bash(3), Write(1), Read(1) |
| 2 | 2026-06-28 | sales-assistant | 執行 **crm-projection** skill：query `nx_client` + deal pipeline → 寫入 `materials/clients/`，cross-ref customer-intel | 17 | Bash(8), Read(5), Skill(1), Write(1) |
| 3 | 2026-06-28 | sales-assistant | 執行 **material-health** skill：檢查 `materials/` 缺漏 frontmatter / 過期補助 / 過時公司資訊 / 孤兒檔案 | 13 | Bash(6), Write(2), Read(2), Skill(1) |
| 4 | 2026-06-28 | sales-assistant | 執行 **crm-projection** skill（同 #2，例行重跑） | 17 | Bash(7), Read(5), Write(2), Skill(1) |

**Top tools**: Bash(24), Read(13), Edit(8), Write(6), Skill(3)

---

## 分析

### 1. 重複的多步驟 workflow
- Session 2 / 3 / 4 的「tool 序列」（Skill → Bash query → Read → Write markdown）本質上**就是既有 skill 的執行軌跡**，不是新的、未被 codify 的人工流程。crm-projection 跑了兩次（#2、#4）只代表它是排程例行任務，符合其 SKILL.md 設計（daily headless agent），不是「應該抽 skill」的訊號。
- Session 1 是一次性個人事件規劃，無重複序列。

### 2. 套用的領域知識
- `nx_client` / `nx_deal` schema 投影、cross-ref customer-intel → **已封裝在 crm-projection skill 內**。
- materials frontmatter / 補助到期 / 公司資訊新鮮度檢查 → **已封裝在 material-health skill 內**。
- 無新的 API pattern / framework convention 被首次揭露。

### 3. 可 codify 的問題解決模式
- 本批次未出現「重複手動操作、尚未成 skill」的模式。

---

## Skill 候選評級

| 候選 | 評級 | 結論 |
|------|------|------|
| 個人生活規劃 → markdown 計畫 | **Weak** | 不抽 |
| CRM 投影流程 | **既有** | crm-projection 已存在，不重複 |
| 素材健檢流程 | **既有** | material-health 已存在，不重複 |

### Weak 候選說明

**名稱（建議不採）**：`life-event-planner` — 把人生事件（求婚、產檢、家族告知、旅程）拆成日程化 markdown 計畫。
- **Purpose**：將模糊的個人時程口述轉成結構化、可勾選的週計畫檔。
- **Trigger**：使用者口述未來一週的私人安排。
- **Category**：—（個人 vault，非工程/自動化領域）。
- **Rationale（為何不採）**：
  1. 落在 **personal vault**，超出 rivendell skills 的領域邊界（rivendell 擁有內部自動化；個人生活規劃不屬任何現有 territory）。
  2. 樣本 n=1，無重複性證據。
  3. 既有的 `planning-with-files` / `task-brief` 已能處理「把模糊交辦拆成結構化計畫」的通用需求，無需專屬 skill。

---

## 結論

**本批次無新 skill 應被建立。**

- Session 2/3/4 證實 **crm-projection** 與 **material-health** 兩個既有 skill 正按設計以例行（含 headless）方式運作——這是 skill 健康運行的正面訊號，而非抽取訊號。
- Session 1 屬個人 vault、單次、且可由通用規劃 skill 覆蓋。

**建議**：維持現狀。若未來 sales-assistant 的 CRM/素材流程出現 skill 外的反覆人工修補（例如手動清理孤兒檔、手動補 frontmatter），再評估擴充既有 skill 而非新建。

> 註：sales-assistant 已標記為 deprecated（見 memory `sales-assistant-deprecated`），正遷移至 chimesflow。此處 skill 的後續演進應跟隨遷移走向評估，不宜在 deprecated 專案上加碼。
