# Session Harvest — 2026-06-22

> 來源：4 個近期 session（~87 則訊息）｜專案：`code`、`rivendell`、`sales-assistant`
> 主要工具：Bash(40)、Read(23)、Write(4)、Agent(3)、Skill(3)、AskUserQuestion(1)

---

## Session 摘要

| # | 專案 | 訊息數 | 核心活動 | 性質 |
|---|------|--------|----------|------|
| 1 | rivendell | 21 | 執行既有 `workflow-retro`（讀 telemetry → 產出 `workflow-retro-2026-W22/W25.md` + LEARNINGS 更新） | 既有 skill 執行 |
| 2 | sales-assistant | 28 | 執行既有 `subsidy-scraper`（grants.nat.gov.tw / SBIR / SIIR 爬取去重、回寫 INDEX.md） | 既有 skill 執行 |
| 3 | sales-assistant | 9 | 執行既有 `crm-projection`（`nx_client` + `nx_deal` 投影到本地 markdown） | 既有 skill 執行 |
| 4 | code | 29 | 從 GitHub repo 列表挑一個 repo → 研究說明（README / CLAUDE.md / DEV_NOTE / PROGRESS）→ 把 `downloads/etl_technical_spec.pdf` 外部技術規格一併納入討論 | 互動式探索（n=1） |

**判讀**：Session 1–3 皆為**既有 skill 的排程/手動執行**（`workflow-retro`、`subsidy-scraper`、`crm-projection` 都已存在於 skills 庫），訊息中直接含 skill 自身的 base directory 與 SKILL.md 內文，確認為正常呼叫，無新 pattern。Session 4 是唯一非 skill-執行的互動：先理解一個陌生 repo、再把一份外部 PDF 技術規格疊進同一場討論——是「repo 上手理解 + 外部規格對照」的綜合動作，但僅出現 1 次。

---

## Skill 候選

### 🔴 Weak — `repo-onboarding-brief`（陌生 repo 上手 + 外部規格對照）

| 欄位 | 內容 |
|------|------|
| **Purpose** | 接手/評估一個不熟的 repo 時的固定動作：拉 GitHub repo 列表挑目標 → 讀 README / CLAUDE.md / DEV_NOTE / PROGRESS 等元文件理出「這 repo 在幹嘛、現況到哪」→ 把一份外部規格文件（PDF/Word，如 `etl_technical_spec`）解析後與 repo 現況交叉對照 → 用 AskUserQuestion 收斂「接下來要怎麼用/改」。輸出一份上手 brief（用途、架構、現況、規格落差）。 |
| **Trigger** | 「研究一下這個 repo 跟我說明」「這份 spec 也納入討論」「幫我看懂這個專案現況」 |
| **Category** | `quality`（或 `workflow`） |
| **Rationale** | 確是可重複的綜合動作，但：(a) **僅 1 次** 出現、單一專案；(b) 與既有能力高度重疊——`github-repo-audit`（repo 結構/文件/CI 健康度評分）、`doc-to-structured-data`（PDF/Word → 結構化）、`context-recovery`（回復工作脈絡）三者已分別覆蓋「理解 repo」「解析外部規格」「收斂脈絡」三段；本 session 只是把三者串在一次對話裡，並無新的領域知識或固定 pipeline。**不建議建 skill**，記為觀察項，待第 2 次「上手 + 外部規格對照」同型 session 再評估是否值得抽一支 orchestrator。 |

---

## 不建議建立（已有覆蓋 / 訊號不足）

- **`workflow-retro` / `subsidy-scraper` / `crm-projection` 的「執行」** → 全為既有 skill 的正常呼叫，非新 pattern。
- **repo 上手 + 規格對照** → `github-repo-audit` + `doc-to-structured-data` + `context-recovery` 已分段覆蓋；單次出現不足以抽 orchestrator skill。

---

## 結論

本批 4 個 session 中，3 個是既有自動化 skill（`workflow-retro`、`subsidy-scraper`、`crm-projection`）的執行紀錄，無新內容；唯一互動式 session（repo 上手 + 外部規格對照）為單次、且可由 `github-repo-audit` / `doc-to-structured-data` / `context-recovery` 組合覆蓋，評為 **Weak / 觀察候選**，不建 skill。**本批無 Strong / Moderate 候選**。

延續 06-15 / 06-18 兩批的趨勢（既有排程 skill 執行占多數、訊號密度低），建議 harvest digest 在前處理階段**過濾掉「session intent 已是某既有 skill 的 base directory / SKILL.md 內文」的純執行紀錄**，只把互動式 session 餵進來，以提升每批的訊號密度與 harvest 效益。
