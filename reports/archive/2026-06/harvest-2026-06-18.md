# Session Harvest — 2026-06-18

> 來源：3 個近期 session（~65 則訊息）｜專案：`Vault-Peter-Work`、`sales-assistant`
> 主要工具：Bash(25)、Read(14)、WebFetch(3)、WebSearch(3)、Write(3)、AskUserQuestion(2)、Skill(2)、Edit(1)

---

## Session 摘要

| # | 專案 | 訊息數 | 核心活動 | 性質 |
|---|------|--------|----------|------|
| 1 | Vault-Peter-Work | 22 | 依「0601 的討論」回溯定位當初的提案規劃簡報素材（議題盤點 / 智慧產線導入規劃 content / 架構圖 v7） | 探索定位（n=1） |
| 2 | sales-assistant | 24 | 執行既有 `subsidy-scraper`（grants.nat.gov.tw / SBIR / SIIR 爬取去重、寫入 INDEX.md） | 既有 skill 執行 |
| 3 | sales-assistant | 19 | 執行既有 `crm-projection`（`nx_client` + `nx_deal` 投影到本地 markdown） | 既有 skill 執行 |

**判讀**：Session 2–3 皆為**既有 skill 的排程/手動執行**（`subsidy-scraper`、`crm-projection` 都已存在於 skills 庫），且 Session 2/3 的訊息中直接包含 skill 自身的 base directory 與 SKILL.md 內文，確認是正常 skill 呼叫，無新 pattern。Session 1 是唯一非 skill-執行的互動，但為單次的「在 vault 裡回溯定位舊素材」，且過程含一次 malformed tool call 與一次 user interrupt，訊號薄弱。

---

## Skill 候選

### 🔴 Weak — `vault-artifact-locator`（依模糊時間/討論線索回溯定位素材）

| 欄位 | 內容 |
|------|------|
| **Purpose** | 在一個累積大量 markdown / HTML 素材的 vault（會議記錄、提案 content、架構圖多版本）中，使用者只給「0601 的討論」「上次那份提案」這種模糊時間/事件線索，要找回正確的那一份檔案並開啟。固定動作：用日期/關鍵字 grep 檔名 → 比對 STATE.md 等索引 → 在多版本（`_v7` 等）中挑最新/最相關 → 必要時 AskUserQuestion 收斂。 |
| **Trigger** | 「幫我開當初那份…」「0601 討論的簡報」「上次提案規劃」「找回之前那份架構圖」 |
| **Category** | `meta`（或 `workflow`） |
| **Rationale** | 確實是一個可重複的探索動作，但：(a) **僅 1 次** 出現、單一 vault；(b) 與既有能力部分重疊——`context-recovery`（回復先前工作脈絡）、`knowledge-graph`、以及單純 `Grep`/`Glob` 已能覆蓋大部分；(c) 本質是「人腦記得大概、用工具收斂」的輕量操作，codify 收益低。**不建議建 skill**，僅記錄為觀察項，待第 2 次出現（尤其若跨不同 vault）再評估。 |

---

## 不建議建立（已有覆蓋 / 訊號不足）

- **`subsidy-scraper` / `crm-projection` 的「執行」** → 全為既有 skill 的正常呼叫，非新 pattern。
- **回溯定位舊素材** → `context-recovery` + `knowledge-graph` + 原生 Grep/Glob 已大致覆蓋；單次出現不足以抽 skill。

---

## 結論

本批 3 個 session 中，2 個是既有自動化 skill（`subsidy-scraper`、`crm-projection`）的執行紀錄，無新內容；唯一互動式 session（vault 素材回溯定位）為單次、輕量、且與 `context-recovery` 重疊，評為 **Weak / 觀察候選**，不建 skill。**本批無 Strong / Moderate 候選**——延續上一批（06-15）「sales-assistant 排程 skill 執行占多數」的趨勢，建議下次 harvest 若仍以既有 skill 執行為主，可考慮把這類 session 從 digest 預先過濾以提升訊號密度。
