我已掌握現有 skill 全貌。以下是分析結果。

---

# Session Harvest Report — 2026-06-17

## Session 摘要

| # | 日期/專案 | 主要活動 | 訊息數 | 工具 |
|---|-----------|---------|--------|------|
| 1 | sales-assistant | 執行 `crm-projection`：查詢 `nx_client` 活躍客戶 → 取 deal pipeline → 交叉比對 customer-intel → 寫 markdown | 17 | Bash(10), Read(3), Skill(1), Edit(1) |
| 2 | sales-assistant | 同上（`crm-projection` 重跑） | 10 | Read(4), Bash(3), Skill(1) |
| 3 | code | 評估 `mops_dbs`（資料庫）與 `TWstock_invest`（前後端）兩 repo 是否整併為 monorepo，用 Agent 程式檢測 → AskUserQuestion 收斂決策 | 9 | Bash(5), Agent(2), AskUserQuestion(1) |

**總計**：3 sessions、~36 訊息、Skills 已用 `crm-projection`×2。

---

## Skill 候選評估

### 🟡 候選 1：`repo-consolidation-advisor`（Moderate）

| 欄位 | 內容 |
|------|------|
| **名稱** | repo-consolidation-advisor |
| **目的** | 評估「多個 repo 是否該整併成 monorepo」的決策。用 read-only Agent 平行檢測兩 repo 的耦合度（共用型別/API contract、import 邊界、framework 版本 drift、部署單元、release 節奏），輸出「建議整併 / 維持分離 / 抽共用 SoT」並附證據與遷移風險清單。 |
| **觸發** | 「兩個 repo 要不要合併」、「整併成 monorepo」、「merge these repos」、「monorepo 評估」、「該拆還是該合」 |
| **分類** | `git/`（與 `repo-rename` 同territory，cross-location 審計 sibling） |
| **理由** | Session 3 完整示範此 pattern：先用 Agent 程式檢測 ground truth，再用 AskUserQuestion 收斂。**現有 skill 沒有覆蓋**——`repo-rename` 只處理改名後的引用審計、`db-migration`/`sqlite-to-postgres` 只處理 DB 引擎遷移，都不是「合併決策」。**但只觀察到 1 次**，且與你 CLAUDE.md 既有原則「共用 SoT > 共用 code（solo dev）」高度相關——這條決策邏輯其實已寫死在 CLAUDE.md「Cross-Repo Strategy」節。 |

**降級為 Moderate 的原因**：n=1（單次出現，未達 skill 化的反覆門檻）；且決策準則已存在於 `~/.claude/CLAUDE.md` 的 Cross-Repo Strategy 段落，可能只需一個薄 skill 把該段落當 SoT 載入 + Agent 檢測，而非全新領域知識。

---

### ⚪ 非候選：`crm-projection`（已存在）

Sessions 1 & 2 只是**執行既有 skill** `workflow/crm-projection`，非新 pattern。觀察到的 `query → cross-ref customer-intel → write markdown SSOT` 流程已被該 skill 與 `markdown-file-ssot` 完整封裝。**無新候選。**

唯一可記錄的觀察：crm-projection 在 2 個相鄰 session 重跑（17→10 msgs），第二次更短——若這是「同日重複觸發」可能值得查是否 headless 排程與手動執行撞車，但這屬 ops 議題非 skill 候選。

---

## 結論

| 評級 | 數量 | 候選 |
|------|------|------|
| Strong | 0 | — |
| Moderate | 1 | `repo-consolidation-advisor` |
| Weak/非候選 | — | crm-projection（已存在） |

**建議**：暫不立即建立 `repo-consolidation-advisor`。理由是 (a) 僅 1 次出現、(b) 核心決策邏輯已在 CLAUDE.md。**待第 2 次「repo 整併評估」需求出現時再抽 skill**——屆時把 CLAUDE.md「Cross-Repo Strategy」節當 canonical SoT，skill 只負責 Step 0 載入該節 + 平行 Agent 程式檢測（耦合度/framework drift/部署單元），符合你既有的「共用 SoT + HARD GATE」模式。

要我把這個「待抽 skill」訊號記到 `rivendell/.learnings/FEATURE_REQUESTS.md` 嗎？這樣下次同類需求出現時能對上第 2 次計數、觸發抽 skill。
