# Session Harvest — 2026-06-24

> 來源：5 個近期 session（~243 則訊息）｜專案：`Vault/Peter-Work`、`mops-dbs`、`sales-assistant`、`tukey-or`
> 主要工具：Bash(115)、Read(29)、Edit(15)、Write(14)、TaskUpdate(6)、AskUserQuestion(5)、Skill(5)、TaskCreate(4)、Agent(2)

---

## Session 摘要

| # | 專案 | 訊息數 | 核心活動 | 性質 |
|---|------|--------|----------|------|
| 1 | Vault/Peter-Work | 60 | 補助案推進：recap「產業競爭力補助」方向 → 審查使用者寫好的「執行方式大綱」→ 產出 `智慧產線導入規劃-content.md`、`研發轉型支持表-結構化.md`、`A1-配貨預測-導入步驟-美珍香試點.md`。屬「補助案計畫書 / 執行方式」撰寫，非 sourcing。 | 互動式撰稿（vault 業務域） |
| 2 | Vault/Peter-Work | 60 | 新北市黑客松 ubike 專案推進：`STATE.md` 任務追蹤 + 多張 dashboard/delivery 截圖（`proj1.png` / `proj1_solved.png`）肉眼驗證。TaskCreate/TaskUpdate 驅動。 | 專案執行追蹤（n=1） |
| 3 | mops-dbs | 71 | 盤點既有爬蟲系統：clone `mops_xbrl_parser`、`mops_price`、`TWstock_invest` → 確認各 repo 爬法 → 產出 `findings.md` + `mops_arch_asis.mmd/png`（現況）與 `mops_arch_target.mmd/png`（目標）兩張架構圖。Skill(3)、AskUserQuestion(3)。 | 既有系統逆向盤點 + as-is/target 架構圖 |
| 4 | sales-assistant | 18 | 執行既有 `crm-projection`（`nx_client`+`nx_deal` → `materials/clients/` markdown，交叉 customer-intel）。訊息直接含 skill base dir 與 SKILL.md 內文。 | 既有 skill 執行（無新 pattern） |
| 5 | tukey-or | 34 | 跑 `requirement` skill 把敘述寫成需求 → 使用者回「太細了，我想知道一個全端開發者看到敘述會怎麼設計整個架構」→ 轉出 `interview-architecture.md`、`operating-workflow.md`、`scheduling-use-case.md`、`solve-pipeline-ordering.md`。 | 既有 skill 觸發但**方向不合**（需求 ≠ 架構設計） |

**判讀**：Session 4 為既有 skill 正常呼叫；Session 2 為單次專案追蹤（已被 `planning-with-files` 覆蓋）。**有訊號的三件**：Session 1（補助案撰寫，業務域既有缺口）、Session 3（既有 repo 逆向 → as-is/target 架構圖，跨專案可複用的盤點動作）、Session 5（使用者明確指出 `requirement` 在「架構設計」這條岔路上不對味，是一個被點破的能力缺口）。

---

## Skill 候選

### 🟡 Moderate — `subsidy-proposal-writer`（補助案計畫書 / 執行方式撰寫）

| 欄位 | 內容 |
|------|------|
| **Name** | `subsidy-proposal-writer` |
| **Purpose** | 把補助案方向（如「產業競爭力補助」「研發轉型」）展開成計畫書核心段落：執行方式、導入步驟、結構化支持表、試點規劃。承接使用者寫好的大綱，補洞並結構化，而非從零生成。 |
| **Trigger** | 「幫我寫補助案的執行方式」「這段產業競爭力補助怎麼寫」「研發轉型支持表結構化」 |
| **Category** | `docs` |
| **Rationale** | CLAUDE.md「Text Report Generation」分類明確記著：`subsidy-scraper` 只負責 **sourcing**，RFP/補助撰寫目前 fallback `/doc-coauthoring`（A 類「RFP ★暫無 skill」）。本 session 正是撰寫側、且是 Peter 反覆出現的真實業務（配貨預測、智慧產線、研發轉型多案並行）。與 `rfq-writer`／`sow-writer`（標案）為同家族但對象不同（補助 vs 標案）。**缺口真實，但 n=1**——建議再蒐集 1 次補助撰稿 session 確認樣板穩定後再抽，先記為候選。 |

### 🟡 Moderate — `legacy-system-arch-audit`（既有系統逆向盤點 → as-is/target 架構圖）

| 欄位 | 內容 |
|------|------|
| **Name** | `legacy-system-arch-audit` |
| **Purpose** | 面對散落的既有 repo（爬蟲 / 資料管線），clone → 逐一確認實作方式 → 輸出 `findings.md` + 一對 Mermaid 架構圖（`*_asis`：現況、`*_target`：目標重構方向），作為重塑 / 整併的決策底圖。 |
| **Trigger** | 「幫我確認這套系統的爬蟲方式」「盤點現有架構畫成 as-is / target」「這幾個 repo 怎麼串的」 |
| **Category** | `quality` |
| **Rationale** | 是可辨識的「逆向盤點」pipeline：多 repo clone → 結構化 findings → 成對 as-is/target 圖。但**幾乎全由既有 skill 組成**：`github-repo-audit`（單 repo 稽核）+ `mermaid-diagram`（出圖）+ `knowledge-graph`。新增價值僅在「多 repo 串接 + as-is/target 成對輸出」這層編排。建議**先擴充 `github-repo-audit` 支援多 repo + as-is/target 雙圖**，而非新建獨立 skill。 |

### 🟡 Moderate — `narrative-to-architecture`（敘述 → 全端架構設計視角）

| 欄位 | 內容 |
|------|------|
| **Name** | `narrative-to-architecture` |
| **Purpose** | 把一段敘述性需求，用「一個全端工程師會怎麼設計整個系統」的視角攤開：資料模型、模組邊界、上下游管線、操作流程、關鍵 use case 與順序依賴——而非 `requirement` 的 user story / 驗收標準粒度。 |
| **Trigger** | 「我想知道全端開發者看到這敘述會怎麼設計架構」「不要驗收標準，給我架構設計」「這段需求的系統怎麼搭」 |
| **Category** | `meta`（或 `workflow`） |
| **Rationale** | Session 5 是使用者**主動點破** `requirement` 在這條岔路不對味（「太細了」），訊號品質高於一般 n=1。但領域已相當擁擠：`gstack-plan-eng-review`（對既有 plan 做架構審查）、`writing-plans`（實作任務）、`task-brief`（🔍探索階段「map options + tradeoffs」）已部分覆蓋「架構設計」。且本 case 與 CLAUDE.md 新增的「Product Evolution（lever-first incremental）」一致——重塑既有產品應**先用 `/user-flow` 攤開旅程**再談架構。建議**先在 `task-brief` 的探索階段路由補一條「→ 架構設計視角」分支或在 `requirement` 加 architecture lens**，而非新建 skill；若再出現 1-2 次「要架構不要需求」的明確岔路，再抽獨立 skill。 |

### 🔴 Weak — 黑客松專案執行追蹤（Session 2）

| 欄位 | 內容 |
|------|------|
| **Purpose** | `STATE.md` + Task 工具 + 截圖肉眼驗證，推進一個競賽型專案。 |
| **Trigger** | 「cd 黑客松專案、繼續推進」 |
| **Category** | `workflow` |
| **Rationale** | 完全落在 `planning-with-files`（STATE/progress 追蹤）+ 截圖 QA 既有覆蓋內。單次、無新 pipeline，**不建**。 |

---

## 結論

本批維持近期低訊號趨勢：5 個 session 中 2 個為既有 skill 正常執行、1 個單次追蹤。**無 Strong 候選**。三個 Moderate 候選共同特徵是「缺口真實但 n=1，且多可由擴充既有 skill 解決」：

1. **`subsidy-proposal-writer`** — 業務域真缺口（撰寫側無 skill），等第 2 次補助撰稿樣本再抽。
2. **`legacy-system-arch-audit`** — 建議**擴充 `github-repo-audit`** 支援多 repo + as-is/target 雙圖，不新建。
3. **`narrative-to-architecture`** — 使用者已點破的能力岔路，建議**先在 `task-brief`/`requirement` 補架構設計分支**，累積到 1-2 次再獨立。

**本期不新建任何 skill**，三項列入觀察 / 擴充既有 skill 的待辦。
