# Session Harvest — 2026-06-02

## 摘要

| 項目 | 數值 |
|------|------|
| 分析 sessions | 3 |
| 訊息數 | ~196 |
| 涵蓋專案 | Vault-Peter、code、code-sales-assistant |
| 主要工具 | Bash(100)、Read(28)、Edit(15)、Write(12)、AskUserQuestion(10)、Skill(4)、Agent(2) |

### Session 逐筆

**[1] Vault-Peter（86 msgs）— 力成 PCB/AOI 驗收標準視覺化**
- 意圖：找甘特圖 → 做成 16:9 視覺化甘特圖 → 「不是啊，請照之前的範本」(timeline 語意修正)
- 活動：把 PCB 檢測驗收規格拆成分階段驗證 pipeline（GATE A 元件分類 ≥95% / GATE B 極性符號辨識 / S2a 間距·MLCC·CSP·板邊豁免 13 條），輸出 `acceptance-gantt.html` + `acceptance-pipeline.html`，並截圖自我檢查 (`gantt-check.png`)。
- 產出檔：`acceptance-gantt.html`、`acceptance-pipeline.html`、`gantt-check.png`、`feedback_gantt_means_timeline.md`、`LEARNINGS.md`
- 工具：Bash(44)、Read(17)、Edit(6)、Write(5)、AskUserQuestion(2)

**[2] sales-assistant（16 msgs）— crm-projection 例行執行**
- 意圖：跑 crm-projection skill，query nx_client/nx_deal、cross-reference customer-intel。
- 活動：純粹執行既有 skill。
- 工具：Bash(10)、Read(3)、Skill(1)

**[3] code（94 msgs）— AutoML 平台（復刻 tukey CLI 成 GUI）**
- 意圖：建立 AutoML 工具，基於 tukey CLI 復刻對應 GUI 與功能（探索分析、演算法切換、時序/分群、最佳化介面）。
- 活動：requirement 定義 → gstack-codex 取得獨立第二意見 (`gstack-codex-oh-tukey.txt`) → 產出多份設計文件。
- 產出檔：`automl-platform-tukey-rebuild.md`、`automl-platform-v1.md`、`manibari-master-design-20260601-190721.md`
- 工具：Bash(46)、Edit(9)、AskUserQuestion(8)、Read(8)、Write(7)、Skill(3)、Agent(2)

---

## Skill 候選評估

> 結論：本批 3 session 中，**2 個已完全落在現有 skill 範圍內**（crm-projection、requirement + gstack-codex），可新增的價值有限。最有提取價值的是 session 3 的「CLI → GUI 功能對等規格」模式（Moderate），其餘為 Weak。

### 候選 1 — `cli-to-gui-parity-spec` 【Moderate】
- **目的**：把既有 CLI 工具（如 tukey）的功能清單系統性對映成 GUI 產品規格 — 逐 subcommand/flag → 畫面、互動、預設值，標記哪些 CLI 能力在 GUI 必須保留、哪些可省略、哪些要重新設計，產出 feature-parity matrix 餵給 requirement / mockup。
- **觸發**：「基於 X CLI 復刻 GUI」「把命令列工具做成介面」「CLI to GUI」「功能對等」。
- **分類**：workflow（與 requirement 相鄰，做為前置 framing）。
- **理由**：session 3 花了大量 AskUserQuestion(8) 在釐清「CLI 有哪些功能、GUI 要不要全做」，這是可重複的決策結構（parity matrix + keep/drop/redesign 三態）。但目前僅出現 1 次、且 requirement 已能承接，故評 Moderate 而非 Strong。**建議：再遇到第 2 個「CLI 復刻成 GUI」案子才抽 skill，先在 FEATURE_REQUESTS 留記。**

### 候選 2 — `inspection-acceptance-pipeline`（製造檢測驗收 → 分階段 gate 圖）【Weak】
- **目的**：把製造業檢測/驗收規格（AOI、SPC、來料檢驗）轉成「分階段驗證 pipeline + 各 gate 通過門檻 (≥95%)」的 16:9 視覺化。
- **觸發**：「驗收標準視覺化」「檢測 pipeline 圖」「AOI gate 圖」。
- **分類**：docs。
- **理由（為何 Weak）**：實質產出（16:9 1600×900、自我截圖檢查、不同概念不同盒）**已被 global CLAUDE.md「Diagram & Slide Output Defaults」+ `chart-design`（system-architecture class）涵蓋**。新 skill 會與 chart-design 重疊，違反「Skills domain boundaries / 別重複」原則。建議不抽。

### 非候選（已存在 / 一次性）
- **crm-projection**（session 2）— 已存在 skill，純執行，無新模式。
- **requirement + gstack-codex**（session 3 的流程骨架）— 兩者皆已存在；session 3 走的正是 CLAUDE.md 既定 backend/product 流程。

---

## 可提取的方法論（建議入 LEARNINGS 而非 skill）

1. **驗證 gate 依「最早可獨立驗證」排序**：session 1 的「純幾何 → 可最早獨立驗 · 復刻率 100%」— 設計驗收/測試 pipeline 時，把不依賴前序結果、最早能單獨跑的檢查放最前（純幾何分類先於語意/極性判讀）。這是跨領域可用的 pipeline 排序啟發法。
2. **「甘特圖」≠ 排程，使用者要的是 timeline 範本**：session 1 的 `feedback_gantt_means_timeline.md` — 客戶說「甘特圖」時先確認是要時間軸視覺化還是真排程，並沿用既有範本而非自創 layout（呼應 global rule「重複 feedback = 方向錯」）。

> 這兩條已分別由 session 1 的 `LEARNINGS.md` / `feedback_gantt_means_timeline.md` 在 Vault-Peter 專案內留存，無需重複登錄至 rivendell。

---

## 行動建議

- [ ] 不新增 skill（本批無 Strong 候選）。
- [ ] 在 `rivendell/.learnings/FEATURE_REQUESTS.md` 記一筆：`cli-to-gui-parity-spec` — 待第 2 個 CLI→GUI 復刻案出現再抽。
- [ ] 方法論 2 條已在來源專案就地留存，本報告僅交叉索引。
