# Session Harvest — 2026-06-04

## 摘要

| 項目 | 值 |
|------|-----|
| 日期 | 2026-06-04 |
| Sessions | 3 |
| Messages | ~80 |
| Projects | `PTI-ARES`, `sales-assistant` |
| Top tools | Bash(33), Read(15), Edit(7), WebSearch(6), Write(4), WebFetch(4), Skill(2) |

### 各 session 活動

1. **PTI-ARES**（30 msgs｜Bash 14, Read 7, Edit 4, Write 1, AskUserQuestion 1）
   - 開發 roadmap 檢視 → 文件整理/更新（README、STATUS、next-wave-2026-05、productization）
   - 規則引擎 rule taxonomy 釐清：59 條 rule 拆成 normal rule / advanced rule 兩層
   - 涉及 `engine.py`，但主軸是文件與分類，不是寫程式

2. **sales-assistant**（17 msgs｜Bash 9, Read 3, Edit 2, Skill 1）
   - **執行既有 skill `crm-projection`**：查 `nx_client` 活躍客戶 → deal pipeline → 交叉比對 customer-intel → 輸出 `materials/clients/`

3. **sales-assistant**（33 msgs｜Bash 10, WebSearch 6, Read 5, WebFetch 4, Write 3）
   - **執行既有 skill `subsidy-scraper`**：爬 grants.nat.gov.tw / SBIR / SIIR → dedup → 寫入補助案 markdown

---

## 關鍵判讀

**Session 2、3 不是新候選，是既有 skill 的正常運轉。**
`crm-projection`、`subsidy-scraper` 都已在 `skills/workflow/` 中。它們被乾淨地觸發並完成，屬於正向訊號（skill 設計有效），不應再開新 skill。本次唯一帶有「新工作型態」的是 Session 1。

---

## Skill 候選

### 候選 1：roadmap-doc-sync — **Weak**

- **Name**: `roadmap-doc-sync`
- **Purpose**: 當專案有多份彼此交疊的狀態/路線文件（README + STATUS + roadmap/next-wave + productization）時，以單一 SoT 為準同步其餘文件，避免進度描述漂移。
- **Trigger**: 「整理一下文件」「把文件都更新」「roadmap 給我看」且專案存在 2+ 份重疊狀態文件。
- **Category**: `meta`
- **Rationale**: Session 1 的主要動作（讀多份文件 → 一致化 → 回寫）是可重複的型態。**但評為 Weak**：
  - 與既有 `sync-readme`、`markdown-file-ssot`、`gstack-document-release` 高度重疊；
  - 僅出現 1 次，無跨 session 重複；
  - 「哪份是 SoT」高度依專案，難抽出通用流程。
  - **建議**：暫不開 skill。若 README/STATUS 同步需求再現 1 次，優先考慮擴充 `sync-readme` 的 scope（涵蓋 STATUS/roadmap），而非新建。

### 候選 2：rule-taxonomy（normal/advanced 規則分層）— **Weak（不建議）**

- **Purpose**: 將大量規則（如 ARES 的 59 條）依複雜度/適用層級分成 normal / advanced 兩層。
- **Rationale**: 這是 **ARES 規則引擎的領域知識**，不是跨專案可重用的工作流。屬於應寫進 PTI-ARES 自身文件/`.learnings/` 的 domain 決策，不是 rivendell skill。**不建議開 skill。**

---

## 結論

本批次 **無 Strong / Moderate 候選**，不新增任何 skill。

- Session 2、3 = 既有 skill 正常運轉，視為健康訊號。
- Session 1 唯一新型態（多文件同步）與既有 `sync-readme` / `markdown-file-ssot` 重疊，且僅一次出現 → 觀察，不急著抽。
- 規則分層為 ARES domain 知識 → 應留在 PTI-ARES 專案內，非平台 skill。

**追蹤項**：若下次再出現「README/STATUS/roadmap 多文件一致化」需求，評估擴充 `sync-readme` 而非新建 skill。
