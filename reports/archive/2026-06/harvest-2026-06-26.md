無 xmind、soft-sensor/PHM、成交歸檔相關 skill。已確認缺口。以下為收割報告。

---

# Session Harvest Report — 2026-06-26

## 概覽

| 項目 | 數值 |
|------|------|
| Sessions | 7 |
| Messages | ~213 |
| 主要工具 | Bash(95)、Edit(40)、Read(33)、Write(9) |
| 涵蓋專案 | Vault/Peter-Work、IC-YMS、PTI-ARES、sales-assistant |

## Session 摘要

| # | 專案 | Msgs | 關鍵活動 |
|---|------|------|---------|
| 1 | Vault/Peter-Work | 51 | PHM 看板（主管版）指標盤點 → 索取數學公式/模型算法 → 要求產出 PDF（嫌公式「難看」） |
| 2 | Vault/Peter-Work | 60 | 「詠鋐/佳世達冰機案已成交」→ 歸檔到資料夾 → 走 `office-xlsx` → Excel 轉 PPT 輸出到 downloads |
| 3 | Vault/Peter-Work | 6 | `phm_soft_sensor.py`（軟測量模型程式） |
| 4 | Vault/Peter-Work | 9 | `AI EMS導入計劃說明.xmind` → 轉 PDF / PNG |
| 5 | IC-YMS | 64 | pull 原始畫面 → UI 對齊 ChimesFlow（已走 `chimesflow-design`） |
| 6 | PTI-ARES | 12 | 背景任務通知處理（純 Bash，無互動意圖） |
| 7 | sales-assistant | 11 | 跑 `crm-projection` skill（已存在） |

> Sessions 5、6、7 已由現有 skill / 機制覆蓋（chimesflow-design、背景任務、crm-projection），不再衍生候選。

---

## Skill 候選

### 🟡 Moderate — `phm-soft-sensor`（PHM 指標與軟測量模型參考）
- **Purpose**：盤點設備健康管理（PHM / Prognostics and Health Management）看板指標，提供每項指標背後的**數學公式 + 模型算法**（軟測量 soft sensor、健康指數、RUL 預估等），並能格式化輸出（公式排版成 PDF，而非裸 LaTeX）。
- **Trigger**：「PHM 指標」「健康指數公式」「軟測量 / soft sensor」「設備健康看板」「RUL 計算」。
- **Category**：`docs/` 或 `backend/`（與 `iot-factory-report` 相鄰但不同：後者做時序圖表報告，本 skill 做指標定義與公式知識）。
- **Rationale**：在 7 個 session 中 **PHM/soft-sensor 出現 2 次**（Session 1 公式、Session 3 程式），是這批裡唯一重複的領域知識。使用者明確痛點：指標有了但**公式/模型缺乏可信來源與美觀輸出**。現有 `iot-factory-report` 不涵蓋指標定義學。需先收集真實公式才能落地，目前知識厚度尚淺，故評 Moderate 非 Strong。

### 🟡 Moderate — `xmind-convert`（XMind → PDF/PNG 轉檔）
- **Purpose**：把 `.xmind` 檔解析並渲染輸出成 PDF / PNG（縮圖、完整心智圖）。
- **Trigger**：「xmind 轉 pdf」「xmind to png」「心智圖匯出圖檔」。
- **Category**：`docs/`（與 `office-pdf`、`mermaid-diagram` 同類的轉檔工具）。
- **Rationale**：Session 4 為單次但**工具型、可重用度高、流程明確**（xmind 是 zip → 取 content.json / thumbnail → 渲染）。現有 office-* 系列無 xmind 支援。單次出現 → Moderate。建議下次再遇到 xmind 需求即抽成 skill。

### ⚪ Weak — `deal-won-intake`（案子成交歸檔）
- **Purpose**：成交案件 → 建立標準資料夾結構 → 把素材（報價/Excel）歸位 → 觸發後續產出（PPT）。
- **Trigger**：「XX 案已成交」「幫我歸檔成交案」。
- **Category**：`workflow/`。
- **Rationale**：Session 2 出現一次，且大部分流程已被 `office-xlsx` / `office-pptx` / `sales-material` / `presales-pipeline` 覆蓋；獨特處僅「成交→資料夾命名約定」一段，厚度不足以單獨成 skill。**建議：併入 `presales-pipeline` 的成交後階段**，不另開。

### ⚪ Weak — `xlsx-to-deck`（Excel 看板 → PPT 輸出）
- **Rationale**：Session 2 的「Excel → PPT → downloads」已可由 `office-xlsx` + `office-pptx` 串接完成，無新模式。**不建議新增。**

---

## 建議

1. **優先觀察 `phm-soft-sensor`**：這是唯一跨 session 重複的訊號，但目前只有「公式 + 一支 .py」。下次再碰 PHM 指標時，先把真實公式/模型蒐齊，再用 `skill-creator` 落地。
2. **`xmind-convert`** 可機會性建立——若你常用 XMind 做計劃書，值得一個穩定轉檔 skill。
3. 成交歸檔 / Excel→PPT 不另開 skill，歸入既有 `presales-pipeline` / office-* 流程。

需要我用 `skill-creator` 把 `xmind-convert` 或 `phm-soft-sensor` 其中一個實際建出來嗎？
