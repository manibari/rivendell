# Session Harvest — 2026-06-23

> 來源：3 個近期 session（~187 則訊息）｜專案：`Vault/Peter-Family`、`PTI-ARES`、`sales-assistant`
> 主要工具：Bash(62)、Edit(40)、Read(36)、Write(10)、Skill(5)、AskUserQuestion(4)、WebSearch(3)

---

## Session 摘要

| # | 專案 | 訊息數 | 核心活動 | 性質 |
|---|------|--------|----------|------|
| 1 | Vault/Peter-Family | 125 | 維護個人五年計劃：得知有寶寶 → 插入產檢/孕期時程、把求婚結婚議題整體向前位移、飲食改原型食物為主。改 `action-plan-flow.md`、`calendar.js`、`design-system.md`、`README.md`、`MEMORY.md` | 互動式（個人 vault，n=1） |
| 2 | PTI-ARES | 49 | 修前端：「尚未開始 / 已完成」視圖卡頓且內容不全 → 截圖（`ares-pending2.png`/`ares-done2.png`）肉眼驗證 → 改 `layout.tsx`、`page.tsx`。含一個 `<task-notification>` 背景任務回收 | 前端 bug 修復 + 截圖 QA（n=1） |
| 3 | sales-assistant | 13 | 執行既有 `crm-projection`（`nx_client` + `nx_deal` 投影到 `materials/clients/` markdown，交叉 customer-intel），訊息直接含 skill base dir 與 SKILL.md 內文 | 既有 skill 執行 |

**判讀**：Session 3 是 `crm-projection` 的正常排程/手動呼叫（已在 skills 庫），無新 pattern。Session 1 是**個人 vault** 的長線計劃維護——訊號雖密（125 訊息），但屬個人生活領域，非可在 rivendell 工程 skills 庫複用的能力。Session 2 是一次前端視圖 bug 的「修 + 截圖驗證」，已被既有 skill 覆蓋。本批仍延續 06-15/18/22 的低訊號趨勢。

---

## Skill 候選

### 🔴 Weak — `cascading-timeline-replan`（錨點變動 → 連動重排時程）

| 欄位 | 內容 |
|------|------|
| **Purpose** | 當一個長線計劃的「錨點事件」改變（如：確認預產期、把婚期整體提前），自動辨識所有相依里程碑並連動位移，維持時程一致性，而非逐項手改。 |
| **Trigger** | 「寶寶來了，產檢時程要提前」「婚期向前，後面全部跟著調」 |
| **Category** | `workflow` |
| **Rationale** | 確是可辨識的「依賴鏈重排」動作，但：(a) **僅 1 次**、且發生在**個人 vault**（生活規劃），非工程 repo；(b) 領域知識（孕期產檢節點、婚禮籌備）不可跨專案複用；(c) 通用的「計劃相依重排」概念已可由 `planning-with-files` / `requirement` 覆蓋。**不建議建 skill**，記為觀察項。 |

### 🔴 Weak — 前端狀態視圖修復 + 截圖驗證

| 欄位 | 內容 |
|------|------|
| **Purpose** | 修前端列表/看板視圖（卡頓、內容不全）→ headless 截圖肉眼比對 → 改 layout/page。 |
| **Trigger** | 「這個視圖很卡、內容也不完整，為什麼」 |
| **Category** | `quality` |
| **Rationale** | 完全落在既有覆蓋內：`gstack-investigate`（root cause）+ `frontend-design`（UI 修）+ `gstack-qa` / `gstack-design-review`（截圖驗證）。單次出現、無新 pipeline，不建。 |

---

## 不建議建立（已有覆蓋 / 訊號不足）

- **`crm-projection` 的「執行」** → 既有 skill 正常呼叫，非新 pattern。
- **個人五年計劃維護** → 個人 vault、生活領域，非工程 skills 庫範疇；通用相依重排由 `planning-with-files` 覆蓋。
- **前端視圖 bug + 截圖** → `gstack-investigate` + `frontend-design` + `gstack-qa` 已分段覆蓋。

---

## 結論

本批 3 個 session：1 個既有 skill 執行（`crm-projection`，無新內容）、1 個個人 vault 生活規劃（領域不可複用）、1 個前端 bug 修復（既有 skill 覆蓋）。**本批無 Strong / Moderate 候選**，兩項皆 Weak / 觀察候選，不建 skill。

延續 06-15/18/22 的建議：harvest digest 前處理階段應**過濾「intent 已是某既有 skill 的 base dir / SKILL.md 內文」的純執行紀錄**（本批 Session 3 即屬此類），並考慮**過濾個人 vault（`Vault/*`）類非工程 session**，以提升每批訊號密度。
