# Session Harvest — 2026-06-14

> 來源：5 個近期 session 摘要 digest | ~254 則訊息 | 估計成本 $0.00
> 工具熱度：Bash(125)、Edit(44)、Read(23)、Write(13)、AskUserQuestion(12)、Agent(3)、Skill(3)、ToolSearch(1)
> 專案：ChimesFlow、Family-Fiscal、rivendell、sales-assistant、vantage-3dgs

## Session 摘要

| # | 專案 | 訊息 | 主要活動 | 關鍵檔案 |
|---|------|------|----------|----------|
| 1 | ChimesFlow | 16 | 「業務計劃打卡」畫面修版 → commit & push（路由用 Agent 輔助） | TodayPlanCard.tsx, page.tsx |
| 2 | Family-Fiscal | 60 | 大量設定/重設工作（「設回 19970521」），改 docker-compose + api + main.py + AGENTS/README | AGENTS.md, README.md, api.ts, docker-compose.yml, main.py |
| 3 | rivendell | 146 | 回顧可創建 skills → 補讀未確認 session → 規劃開發 roadmap + 版本/ports（VERSION/CHANGELOG/ROADMAP 落地） | CHANGELOG.md, CLAUDE.md, FEATURE_REQUESTS.md, MEMORY.md, ROADMAP.md |
| 4 | sales-assistant | 13 | 執行既有 `crm-projection` skill（查 nx_client → pipeline → 交叉 customer-intel → 寫 markdown 索引） | INDEX.md, clients.py, projection.md |
| 5 | vantage-3dgs | 19 | 記錄設計決策（tukey 系列下游運算 / automl-bi 優先）→ 標記 ROADMAP 優先序 + 寫 architecture | CLAUDE.md, MEMORY.md, ROADMAP.md, architecture.md, tukey-downstream-compute.md |

**整體訊號評估**：Session 4 為既有 `crm-projection` 的正常排程使用（且 Session 3 中段亦含「回顧 skills」的 meta 工作），無新模式。Session 1（UI 修版）、Session 2（設定重設）屬一次性維護。可萃取的「新」可重用模式集中在 **Session 3 與 Session 5 共同浮現的「決策捕捉 → ROADMAP + 版本治理」工作流**——兩個獨立專案（rivendell、vantage-3dgs）同週都走了「把進行中的設計/優先決策固化進 ROADMAP.md，並維護 VERSION/CHANGELOG/MEMORY」的相同序列，這是本批次少數 N≥2 的重複訊號。

---

## Skill 候選

### 1. `roadmap-decision-capture` — **Moderate**

- **Purpose**：把進行中的設計/優先決策「固化」成持久的專案治理文件——讀現有 ROADMAP.md → 將決策（例：tukey 下游運算、automl-bi 優先、ports/version 規劃）寫入並標記優先序（P0/OR 優先）→ 記錄 rationale 到 MEMORY.md / CLAUDE.md → 視需要 bump VERSION + 補 CHANGELOG 條目 → 留下 `docs/plans/YYYY-MM-DD-*.md` 決策記錄。一條穩定的「decision → roadmap → version → changelog」管線。
- **Trigger**：「標進 roadmap」「ROADMAP 標一下優先」「記錄這個設計決策」「bump 版本 + 補 changelog」「規劃版本/ports」，或使用者描述一個方向決策後要求「記住/標優先」。
- **Category**：`meta`
- **Rationale**：Session 3（rivendell：ROADMAP + VERSION + CHANGELOG + docs/plans 一次落地）與 Session 5（vantage-3dgs：設計決策 → ROADMAP 優先序 + architecture）是**兩個獨立專案的同型工作流**，N=2 真實重複訊號。與既有 skill 邊界清楚：`gstack-document-release` 是 ship 後更新 docs/README、`sync-readme` 只同步 README、`session-wrap` 是收工清理、`workflow-retro` 是週回顧；**沒有任何 skill 負責「決策進行式 → roadmap/版本治理」的持續維護**。`app-ops-baseline` 只把「roadmap/changelog」列為新 app 的 PLANNED 檢查項（規劃 gate），不負責後續維運——本候選正好補上 baseline 之後的 operating 動作。**列 Moderate 而非 Strong**：兩次都是 solo meta 維護、digest 摘要被截斷無法看到完整步驟。建議先沉澱 `roadmap-conventions.md` SoT（優先序標記法、VERSION/CHANGELOG 格式、docs/plans 命名），第三案再正式 skill 化。

### 2. （不抽，餵回既有 skill）Family-Fiscal 設定重設 / docker 重整 — **Weak**

- **觀察**：Session 2 用 22 Bash + 21 Edit 改 docker-compose + api + main.py + AGENTS/README，意圖「設回 19970521」語意不明（疑似重設某個 seed/密碼/初始值）。重編輯量大但屬單專案一次性設定校正。
- **建議**：不抽新 skill。docker/環境校正若反覆出現，既有 `docker-compose-setup` 與 `env-doctor` 已覆蓋；若是密碼/seed 重設則屬一次性。無跨 session 重複模式。

### 3. （不抽）ChimesFlow UI 修版 — **Weak**

- Session 1 為單元件版面修正後 commit/push，屬日常 frontend 維護（`frontend-design` / `gstack-review` 已涵蓋），無新可重用模式。

### 4. （不抽）crm-projection 排程使用 — **Weak**

- Session 4 為既有 `crm-projection` skill 的正常排程執行（digest 顯示已正確 Skill 路由），屬日常使用，無新模式。

---

## 行動建議

1. **`roadmap-decision-capture`** 暫緩抽 skill：先在 rivendell 建 `docs/roadmap-conventions.md` SoT（優先序標記、VERSION/CHANGELOG 格式、`docs/plans/` 命名慣例），把 Session 3 已落地的格式固化為慣例；累積第三案證據再 skill 化（歸 `skills/meta/`）。
2. 本批次無 Strong 候選——digest 摘要嚴重截斷（intent 多被切斷、無 skill body），訊號偏弱；多數 session 已由既有 skill 正確涵蓋，屬「skills library 已成熟」的健康訊號。
3. 與 2026-06-13 harvest 的 `doe-ml-analysis`（Strong，命中製造運營 domain gap）併看：vantage-3dgs 的 tukey 下游運算亦屬製程/材料分析方向；若 `doe-ml-analysis` 抽出，vantage 的下游運算需求可一併納入其 scope 評估。
