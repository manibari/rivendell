# Session Harvest — 2026-06-12

> 來源：5 個近期 session 摘要 digest | ~53 則訊息 | 估計成本 $0.00（皆為低成本短 session）
> 工具熱度：Bash(25)、Read(16)、Edit(4)、Skill(1)

## Session 摘要

| # | 專案 | 訊息 | 主要活動 | 關鍵檔案 |
|---|------|------|----------|----------|
| 1 | Vault-Peter | 6 | `cd` 到光泉專案、開啟工作狀態畫面 | STATE.md |
| 2 | PTI-ARES | 9 | CLI 除錯 / 操作（Bash 為主，無 intent 文字） | cli.py |
| 3 | sales-assistant | 10 | 執行既有 `crm-projection` skill：查 nx_client active clients、交叉比對 deal pipeline | projection.md |
| 4 | tukey-bi | 14 | BI roadmap (B)：通用 loader + 時序 view，研讀多份 config | config.py, config.yaml, data.py, index.html, instance-spec.md |
| 5 | code (3dgs) | 14 | 文件漂移修補：版本對齊到 v0.12，跨多份文件同步 | CHANGELOG.md, CLAUDE.md, ROADMAP.md, progress.md |

**整體訊號評估**：本批 digest 訊號偏弱。Session 1–3 為既有工具/skill 的日常使用（crm-projection 已存在、PTI-ARES 缺 intent 文字、光泉純導航），可萃取的「新」可重用模式集中在 Session 4–5。

---

## Skill 候選

### 1. `doc-drift-sync` — **Moderate**

- **Purpose**：版本升版（如 v0.11 → v0.12）時，跨多份專案文件（CHANGELOG.md / ROADMAP.md / CLAUDE.md / progress.md）一致性對齊 — 偵測哪些檔案版本號／狀態落後，補齊並交叉檢查彼此不矛盾（roadmap 已完成項是否寫進 changelog、progress 是否反映 roadmap）。
- **Trigger**：「更新文件」「版本對齊」「文件漂移」「bump version docs」「同步 CHANGELOG/ROADMAP」，或偵測到 CHANGELOG 最新版本與 package/git tag 不一致。
- **Category**：`docs`（或 `meta`）
- **Rationale**：Session 5 的核心動作正是「一次 Read 3 份、Edit 4 份」的多文件版本協調 — 這是 solo dev 跨專案反覆出現的維護工作。現有 `sync-readme` 只從 skills 反推 README，`gstack-document-release` 偏 release notes，沒有一支專門處理「跨內部文件版本漂移」的 skill。**訊號來自單一 session，建議再觀察 1–2 次同模式出現後再抽出**（避免過早 N=1 抽 skill）。

### 2. `config-driven-bi-instance` — **Weak→Moderate**

- **Purpose**：以 `config.yaml` + `instance-spec.md` 驅動的「通用 loader + 時序 view」BI 樣板 — 新增一個 BI instance 時不改 code，只填 config（資料來源、欄位 mapping、時序維度），由通用 `data.py` loader 載入並渲染 `index.html`。
- **Trigger**：「新增 BI instance」「通用 loader」「config 驅動的儀表板」「時序 view」。
- **Category**：`workflow`
- **Rationale**：Session 4 展現了「一份 canonical config SoT → 多 instance 消費」的模式，與全域 CLAUDE.md「共用 SoT 文件 + 各 repo 各自實作」策略一致。**但目前高度綁定 tukey-bi 單一專案**，缺乏跨專案複用證據；歸為 Weak→Moderate，等第二個專案採用同模式再考慮 skill 化。可先以 `markdown-file-ssot` / `instance-spec.md` 樣板形式沉澱。

### 3. （不抽）CLI 操作除錯 — **Weak**

- Session 2（PTI-ARES, Bash×7 on cli.py）無 intent 文字、純互動除錯，已被既有 `gstack-investigate` 涵蓋。不建議新 skill。

### 4. （不抽）專案導航 / 開啟狀態畫面 — **Weak**

- Session 1（Vault-Peter, `cd` + Read STATE.md）為單步導航，過於瑣碎；若要沉澱屬 `context-recovery` 範疇，已存在。不建議新 skill。

---

## 結論

- **無 Strong 候選**：本批 session 多為既有工具的日常使用，新模式樣本數不足（多為 N=1）。
- **唯一值得追蹤**：`doc-drift-sync`（Session 5）— 模式清晰且填補現有 docs skill 空缺，但建議等再出現 1 次跨文件版本對齊需求即抽出。
- **既有 skill 已覆蓋**：crm-projection（S3）、investigate（S2）、context-recovery（S1）。
- 已對照 `/Users/manibari/code/rivendell/skills/` 全 7 類別 84 支 skill，上述候選均不與現有 skill 重複。
