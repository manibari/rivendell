# Session Harvest — 2026-06-25

> 來源：3 個近期 session（~80 則訊息）。涵蓋專案：`sales-assistant`、`tukey-bi`。
> 收割目標：找出可沉澱成 skill 的重複工作流／領域知識／問題解法。

## Session 摘要

| # | 專案 | 訊息 | 主要活動 | Top 工具 |
|---|------|------|----------|----------|
| 1 | sales-assistant | 13 | 執行 `crm-projection` skill：查 `nx_client` 活躍客戶 → 撈 deal pipeline → 對照 customer-intel 報告 → 投影成 `materials/clients/` markdown | Bash(8), Read(2), Skill(1) |
| 2 | sales-assistant | 28 | 執行 `subsidy-scraper` skill：爬 grants.nat.gov.tw / SBIR / SIIR → dedup → 寫入/封存補助案檔 → 重生 INDEX.md | WebSearch(7), Bash(6), WebFetch(6), Read(4), Skill(1) |
| 3 | tukey-bi | 39 | 把單一 app 重塑成「多應用平台」：外層加 Projects 清單 + sidebar 導覽；改 `config.py`、`main.py` | Bash(26), Edit(8), Read(2), AskUserQuestion(1) |

**整體判讀**：Session 1、2 是**既有 skill 的正常執行**（`crm-projection`、`subsidy-scraper` 都已存在於 `skills/workflow/`），不是新 candidate——它們的價值是「驗證既有 skill 仍可運作」的訊號，不需新建。真正具 skill 化潛力的只有 Session 3。

---

## Skill Candidates

### 🟡 Moderate — `multi-app-shell-scaffold`（多應用平台外殼骨架）

- **Purpose**：把一個「單一功能 app」升級成「平台」時，產出標準外殼：(a) Projects/Apps landing 清單頁、(b) 持久化 sidebar 導覽、(c) 各 app 掛載點與路由切分。重點是**接線（wiring）而非重寫**——多數畫面已存在，缺的是外殼與導引。
- **Trigger**：使用者說「這應該是其中一個應用」「外面要有 Projects」「需要一個 sidebar 吧」「把這個變成平台」「multi-app」「app launcher」。
- **Category**：`frontend`（或 `workflow`，視實作偏重）。
- **Rationale**：
  - 直接命中 CLAUDE.md「Product Evolution Strategy (Incremental Lever-First)」——重塑既有產品前**先用 `/user-flow` 攤開完整旅程**、找接線槓桿、地基不動。此 candidate 可把該策略從「規則」落成「可執行 skill」：強制 Step 0 跑 user-flow → 標出 Projects/sidebar 是外殼（非 lever）→ 增量接線。
  - 但**僅來自單一 session、無跨 session 重複**，且 config.py/main.py 的具體做法可能高度綁定 tukey-bi 技術棧（Python 後端）。需再觀察 1–2 次同類任務確認模式穩定，才值得正式建檔。
  - 既有 skill 缺口確認：`app-ops-baseline`（只管 ops 功能清單）、`chimesflow-design`（設計 SoT gate）、`user-flow`（畫流程不搭骨架）、`frontend-design`（產畫面不管 IA 重塑）——**皆未覆蓋「單 app → 多 app 平台外殼重塑」這個 IA 層級的工作流**。

---

### ⚪ Weak — 不建議建檔（記錄理由備查）

- **`crm-projection` / `subsidy-scraper` 的「執行紀錄」**：兩者皆已是成熟 skill，session 只是排程式執行，無新模式。→ 不建。
- **「DB 查詢 → 對照報告 → 投影 markdown SSOT」通用流程**（Session 1）：看似可抽象，但已被 `crm-projection`、`material-health`、`markdown-file-ssot`（backend）覆蓋。再抽一層會與既有 skill 重疊。→ 不建。
- **「爬政府來源 → dedup → 寫檔 → 重生 INDEX」通用流程**（Session 2）：已有 `subsidy-scraper`、`tender-scraper` 兩個實例 + CLAUDE.md「Skill structure preference」傾向「一個 orchestrator + data files」。若未來出現第 3、第 4 個同型爬蟲，應考慮**抽共用 orchestrator**，而非各自為政；目前 2 個尚不足。→ 暫不建，列為觀察項。

---

## 後續觀察項（watch-list，非 action）

1. **`multi-app-shell-scaffold`**：若再出現 1 次同型「把 app 變平台」任務，即升級為 Strong 並交 `skill-creator` 建檔。
2. **政府爬蟲 orchestrator 收斂**：`subsidy-scraper` + `tender-scraper` 已是 2 個實例，第 3 個出現時觸發抽共用層的評估（對映 CLAUDE.md skill-structure-preference）。

---

*Generated by session-harvest review. 本報告為 read-only 分析，未建立任何 skill。*
