# Session Harvest — 2026-06-01

> 來源：3 個近期 session 的預摘要 digest（~135 則訊息）。
> 目的：找出可重用、值得抽成 skill 的多步驟工作流 / 領域知識。

## Session 摘要

| # | 日期 | 專案 | 主要活動 | 訊息數 | 主要工具 |
|---|------|------|----------|--------|----------|
| 1 | 2026-06-01 | `Vault-Peter` (Obsidian) | 查看既有提案書 → 診斷 Obsidian 打不開 → 關閉重開並指向正確 vault；產出錦選洋(jxy) ABCD 提案 HTML/PNG | 78 | Bash(41) Edit(8) Read(7) Write(5) AskUserQuestion(2) |
| 2 | 2026-06-01 | `code/sales-assistant` | 執行 `crm-projection` skill — 查 `nx_client` 活躍客戶、deal pipeline、交叉比對 customer-intel，投影到本地 markdown | 20 | Bash(10) Read(5) Edit(2) Skill(1) |
| 3 | 2026-06-01 | `code/sales-assistant` | 執行 `subsidy-scraper` skill — 爬 grants.nat.gov.tw / SBIR / SIIR、去重、寫檔、重建 INDEX.md | 37 | Bash(18) WebFetch(5) Read(4) WebSearch(3) Edit(3) |

**整體判斷：本次 harvest 偏薄。** Session 2、3 是「執行既有 skill」（`crm-projection`、`subsidy-scraper` 都已存在且運作正常），不產生新 candidate。唯一有新 pattern 的是 Session 1。

---

## Skill Candidates

### 1. `obsidian-vault-doctor` — Moderate（偏 Weak，單次出現）

- **Purpose**：診斷 macOS 上 Obsidian「打不開 / 開到錯誤 vault」的問題，並安全地關閉重啟、重新指向正確 vault。涵蓋：找出 Obsidian 目前載入的 vault 路徑（`~/Library/Application Support/obsidian/obsidian.json`）、確認 vault 是否落在 iCloud Documents（已知 landmine）、判斷是程序卡死還是 vault 路徑錯誤、`osascript`/`open -a` 重啟指向指定 vault。
- **Trigger**：「Obsidian 打不開」「開到錯的 vault」「幫我重開 Obsidian 指向正確 vault」「vault 找不到筆記」。
- **Category**：`meta`（本機環境維運）或 `workflow`。
- **Rationale**：
  - Peter 把提案書 SSOT 放在 Obsidian Vault（`Vault-Peter` 本身就是一個常駐專案），這個故障點會反覆出現。
  - 與既有 CLAUDE.md「iCloud Documents Sync ≠ source tree」landmine **直接相關**——vault 在 iCloud 下會被 file-provider 接管、產生 `file 2.md` 衝突副本、多設備 mtime 不同步，正是「打不開 / 開到舊版」的常見根因。把這條已知規則 codify 成可執行的 doctor 腳本有複利價值。
  - **保留意見**：本次僅 1 次出現，且大半是 macOS app 操作而非可泛化的程式邏輯。建議**先記為 candidate、等第 2 次踩到再抽 skill**（符合「fired ≥2× 才升級」的既有門檻）。

### 2. ABCD-framework 客戶提案 HTML/PNG 產生器 — Weak（已被既有能力覆蓋）

- **Purpose**：用官方計畫書/SOW 的 ABCD 分項當頂層 framing，產出 16:9 報告級提案 HTML 並自我截圖檢查（產物：`jxy_abcd.html` + `jxy_abcd.png`）。
- **Trigger**：「做 ABCD 提案」「客戶提案視覺化」。
- **Category**：`docs` / `workflow`。
- **Rationale（為何 Weak）**：此流程**已被既有資產完整覆蓋**：
  - `~/.claude/CLAUDE.md` 的「Diagram & Slide Output Defaults」已寫死 16:9 1600×900、官方文件 framing 優先、ABCD overlay、生成後 headless Chrome 自我截圖驗證等規則。
  - `chart-design`（架構圖/表格 sub-workflow）+ `gstack-design-html` 已負責實際產生。
  - 不建議新增 skill；屬於既有 routing 已處理的案例。若要強化，應是**補一個 ABCD-proposal 的 style file 到 `chart-design/styles/`**，而非新 skill。

### 3. Scraper 去重 + ERRORS.md 韌性記錄 pattern — Weak（已被 `subsidy-scraper`/`tender-scraper` 覆蓋）

- **觀察**：Session 3 編輯了 `ERRORS.md`（3 次），顯示爬蟲過程中有錯誤記錄與重試。
- **Rationale**：`subsidy-scraper` 與 `tender-scraper` 的 SKILL 已內建去重、archive、INDEX 重建、retry/backoff 與 dashboard observability。無新 pattern 可抽。**不建議新增。**

---

## 結論與建議

- **本次不建議立即新增任何 skill。** 3 個 candidate 中，2 個已被既有 skill / CLAUDE.md 規則覆蓋，1 個（`obsidian-vault-doctor`）僅單次出現。
- **追蹤項**：把 `obsidian-vault-doctor` 列入觀察名單，與既有「iCloud Documents Sync」landmine 綁定；**第 2 次踩到 Obsidian vault 故障時即抽 skill**，並可直接複用 CLAUDE.md 已備好的 `~/.local/bin/icloud-ignore-build-dirs` idempotent 思路。
- **可選微改善**：若 ABCD 提案會持續做，替 `chart-design` 補一個 `styles/abcd-proposal.md`（report 級主標籤、16:9、官方章節代碼當頂層），比新 skill 划算。

_Harvest 偏薄屬正常——多數 session 是執行既有 skill，正是 skill library 成熟的訊號。_
