# Session Harvest — 2026-07-08

> 來源：4 個近期 session 的預先摘要 digest（~723 msgs）。
> 已比對現有 skills（`skills/` 7 大分類，130+ skills）與 `~/.claude/CLAUDE.md` 路由表，僅列出**尚不存在**的候選。

## Session 摘要

| # | 專案 | 訊息數 | 主要活動 |
|---|------|--------|----------|
| 1 | PTI-ARES | 525 | S1 距離/架構 rule 檢查；發現 rule 對帳前必須回頭重建「元件本身定義」的 SoT（`build_component_sot.py`、`checklist.py`、`cp0030-reconciliation-plan.md`）；重度 Bash(368)+Edit(113)+Task 追蹤 |
| 2 | PTI-ARES | 83 | 客戶反應週報「非常人能讀」→ 改寫為敘述性說明 → `build_pptx.py` 產 PPTX → 逐頁截圖檢查 → 與甘特圖對齊供口頭報告 |
| 3 | verdandi-or | 39 | 讀客戶 PDF（基礎資料用於 AI 排程）+ 現場照片 → 數位化為 `digitized-data.json` → 評估現有 OR 系統能否支援 → `jobshop-simulation-report.md` 可行性報告 |
| 4 | code（IC YMS deck） | 76 | IC 半導體 sales deck HTML 迭代；「字太小、要看到 distribution、落 bin 預測分析是重點」→ 逐頁截圖（p1–p11）修正循環 |

**跨 session 重複模式**
1. **截圖自檢循環**（session 2、4）：產出 HTML/PPTX → headless 截圖 → Read PNG → 修字級/溢出 → 再截圖。與 `~/.claude/CLAUDE.md`「生成圖後一律自我截圖檢查」規則一致，但目前是散文規則、不是可執行 skill。
2. **工程產出 → 客戶可讀轉譯**（session 2、4）：兩個不同客戶都回饋「看不懂/字太小」，都需要把工程細節重新 framing 成敘述性、對齊客戶已有的參照物（甘特圖、良率 distribution）。
3. **紙本/PDF 客戶資料 → 結構化 → 模擬驗證**（session 3）：digitize → simulate → 報告，對應 CLAUDE.md 文字報告路由表中「排程 / 產能規劃 ★ 暫無 skill」的已知缺口。

---

## Skill 候選

### 🟢 Strong — client-weekly-report（客戶週報敘述化）

- **目的**：把工程週報（commit/task 流水帳）轉譯成客戶高層可讀的敘述性進度報告：本週做了什麼、為什麼重要、下週往哪走，並強制對齊專案既有的時程參照物（甘特圖 phase / milestone 編號），可選輸出 PPTX（office-pptx sub-call）+ 逐頁截圖自檢。
- **Trigger**：「撰寫週報」「客戶週報」「進度報告給客戶」"weekly status report"。
- **分類**：`docs/`
- **Rationale**：Session 2 是完整的一次踩坑—修正循環：客戶明確回饋「非常人能讀」，修正方向（敘述性 + 對齊甘特圖）已被驗證。現有 `internal-comms` 只管公司內部公告、`de-slopify` 只管文字打磨、`office-pptx` 只管輸出格式——「工程→客戶」的 framing 轉譯層沒有 skill。PTI-ARES 是每週固定交付，重複頻率有保證。

### 🟡 Moderate — jobshop-feasibility（排程可行性評估）

- **目的**：接收客戶排程基礎資料（PDF/照片/Excel）→ 數位化為結構化 JSON（機台、工序、工時、限制）→ 對照現有 OR 系統能力矩陣跑 jobshop 模擬 → 產出「能不能支援、缺什麼」的可行性報告。
- **Trigger**：「這個排程需求我們的系統能不能做」「排程可行性」「jobshop 評估」。
- **分類**：`workflow/` 或 `backend/`
- **Rationale**：Session 3 走完整條 pipeline（digitize → simulate → report），且直接命中 CLAUDE.md 文字報告路由表 D 分支「排程 / 產能規劃 ★ 暫無 skill」的已登記缺口。降為 Moderate 的原因：目前只出現 1 次，且前段 digitization 與現有 `doc-to-structured-data` 重疊——建議做成薄的 orchestrator（路由到 doc-to-structured-data + verdandi-or 模擬腳本），符合「orchestrator + data files」的既有偏好。等第二個排程案子出現再抽。

### 🟡 Moderate — deck-visual-qa（簡報逐頁截圖稽核）

- **目的**：把 CLAUDE.md「Diagram & Slide Output Defaults」的散文規則（16:9 1600×900、字級基準表、<12px 必須 justify、溢出/wrap/空白塊檢查）codify 成可執行的稽核 skill：對 HTML deck / PPTX 逐頁 headless 截圖 → Read PNG → 按 checklist 逐項判定 → 輸出修正清單。
- **Trigger**：「檢查這份 deck」「字太小」第 1 次出現時、或任何 deck 生成 skill 的收尾 gate。
- **分類**：`quality/`
- **Rationale**：Session 2 和 4 各自手動跑了同一套截圖—檢查—修正循環（兩案合計 30+ 張 PNG 往返）。規則已存在但每次都靠散文提醒重新執行，工具序列固定（Chrome headless → Read → Edit → 再截圖），是典型可 codify 的 mechanical loop。降為 Moderate 的原因：與 `gstack-design-review`（視覺一致性審查）部分重疊，建議先確認能否以「deck 模式」擴充 gstack-design-review，而不是新開 skill（skills domain boundaries 規則）。

### 🔴 Weak — component-sot-reconciliation（元件定義 SoT 對帳）

- **目的**：rule 檢查結果異常時，先回頭重建每個元件本身定義的 single source of truth，再對帳 rule 輸出。
- **Trigger**：DFM/rule 檢查數字對不上時。
- **分類**：`backend/`
- **Rationale**：Session 1 的核心教訓（「你應該要回去看每個元件本身的定義」）是有價值的 debugging 原則，但實作（`build_component_sot.py`、cp0030）高度綁定 PTI-ARES 的資料模型，且概念層已有 `markdown-file-ssot` + `odb-dfm-reference` + CLAUDE.md「Trust ground truth, not derived/cached state」規則覆蓋。**不建議建 skill**；建議改記一條 PTI-ARES 專案的 `.learnings/LEARNINGS.md`（category: best_practice）：「rule 對帳異常 → 先重建元件定義 SoT 再比對，不要在 rule 端調參」。

---

## 已覆蓋、不需新 skill

- **Session 4（IC YMS sales deck）**：路由已存在 — `sales-deck-design` + `slide-workflow` + `gstack-design-html`；「字太小」重複回饋已由 CLAUDE.md「重複 feedback 是方向錯訊號」規則覆蓋（本次為第 2 次出現於 IC YMS 案，應觸發結構性 redo 而非字級微調）。
- **Session 3 前段 PDF 數位化**：`doc-to-structured-data` 已覆蓋。
- **Session 1 的計畫檔驅動長工**：`planning-with-files` 已覆蓋。

## 建議下一步

1. 用 `/skill-creator` 建 `docs/client-weekly-report`（Strong，本週即有下次週報可 dogfood）。
2. `jobshop-feasibility` 與 `deck-visual-qa` 先掛入 `.learnings/FEATURE_REQUESTS.md` 觀察名單，前者等第二個排程案、後者先評估併入 `gstack-design-review`。
3. PTI-ARES 專案 `.learnings/` 補記 SoT-first 對帳教訓（session 1）。
