已完成分析，報告寫在 `reports/harvest-2026-07-21-session-digest.md`。

**結論先講**：20 場 session 裡最強的訊號是 mops-dbs-services-mops-notes 專案裡，同一段 F17「前十大股東相互間關係表」抽取 prompt 被逐字手貼了 **16 次**，Tools 欄幾乎全空——代表這是純聊天手動流程，還沒收斂成可重複呼叫的介面。

三個候選：
- 🟢 **Strong** — `mops-shareholder-relation-extract`：把這個重複 16 次的 prompt+schema 收成 skill。額外查證發現 `mops_dbs/services/mops_notes/sync/shareholder_llm.py` 裡已有幾乎相同的 prompt 常數與 JSON 容錯邏輯，代表這模式已驗證有效，只是還沒被介面化——skill 的價值是統一 schema、內嵌現有容錯邏輯，並註明「批次回填走 `shareholder_llm.py`，單筆才用這個 skill」。
- 🟡 **Moderate** — 背景建置+截圖驗證除錯模式：只有 1 場證據、細節不足，先不造，建議觀察是否再出現才決定。
- ⚪ **Weak** — JMP Essentials 研究彙整：這模式已被既有 `deep-research`/`autoresearch` skill 覆蓋，不建議重複造輪子。

其餘 2 場（MingOS-engine、mops-dbs）訊息太短、摘要無意圖文字，無法判斷 workflow，未列入候選。
