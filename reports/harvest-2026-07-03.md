# Session Harvest — 2026-07-03

> 來源：3 個近期 session（~154 訊息）｜比對基準：rivendell/skills（7 類 145+ skills）
> 結論：**訊號偏弱**。三個 session 的核心活動大多已被既有 skill 覆蓋，僅 1 個 Moderate 候選（IC 產業領域知識），其餘為 Weak / 直接歸檔 learnings。

---

## Session 摘要

| # | Project | 訊息 | 主要活動 | 關鍵工具 | 產出檔案 |
|---|---------|-----|---------|---------|---------|
| 1 | Vault/Peter-Work | 55 | 為 IC-YMS / IC 產業痛點寫 sales deck，把 ML 能力（異常檢測 / 根因分析 / 良率預測 / 參數最佳化）對映到產業痛點，審 storyline | Read(23) Bash(16) Edit(6) AskUserQuestion(1) TaskCreate(1) | `ic-yms-sales-deck.html`, `STATE.md` |
| 2 | code/news-stock | 83 | 查修 Nuxt 前端 → `git add web/pnpm-lock.yaml` 一起 commit → 查詢 portfolio（WOLF 部位、月漲幅） | Bash(58) Read(6) Edit(6) AskUserQuestion(2) ToolSearch(2) | `LEARNINGS.md`, `current-work.md`, `nuxt_*.png` |
| 3 | code/sales-assistant | 16 | 執行 `crm-projection` skill：query nx_client → deal pipeline → cross-ref customer-intel → 寫 markdown | Bash(8) Read(5) Skill(1) | `INDEX.md`, `clients.py`, `deals.py`, `projection.md` |

---

## Skill 候選

### 🟡 Moderate — `ic-yms-solution-narrative`（IC 良率管理解決方案敘事）

- **Purpose**：把半導體 / IC 產業（尤其 YMS 良率管理）的**產業痛點 → ML 能力**對映固化成一份 reference：異常檢測、根因分析（RCA）、良率預測、參數最佳化四大主軸各自對應的痛點語言、決策者角色、量化效益 framing。供 `sales-material` / `slide-workflow` / `pitch-deck` 在做 IC 客戶 deck 時當領域素材抽用。
- **Trigger**：「IC / 半導體 / 晶圓 / YMS / 良率 sales deck」「fab 客戶提案」「wafer yield / RCA / APC 簡報」。
- **Category**：`docs`（reference-type，餵給既有 deck 流程）。
- **Rationale**：Session 1 花 55 訊息在「痛點↔ML 能力對映」與 storyline，這層**領域知識**是可複用的槓桿，且目前沒有 IC 業務敘事類 skill（`ic-lot-normalization`、`odb-dfm-reference` 都是技術 reference，非 sales framing）。
- **保留意見**：deck 產製流程本身已有 `slide-workflow` + `slide-office-hours` + `sales-material` 完整覆蓋，此候選**只該是資料/reference 檔**，不是新流程 skill——符合「orchestrator + data files」偏好（[[skill_structure_preference]]）。**n=1，建議接到第 2 個 IC-YMS 真實案子再抽**，先記入 domain gap 追蹤（呼應 CLAUDE.md「製造運營暫無 skill，接到真實案子時抽」）。

### 🔴 Weak — Nuxt 前端除錯（不建議建 skill）

- Session 2 的 Nuxt 查修屬一次性 ad-hoc 除錯，已被 `gstack-investigate`（系統化根因）覆蓋；portfolio 查詢走既有 `investment-research` + news-stock MCP。
- 唯一可複用片段：`pnpm-lock.yaml` 需與 code 同批 commit（lockfile 選 package manager）——這是**通用 gotcha，非 skill**，且 CLAUDE.md「Engineering Gotchas」已收錄「Lockfile picks the package manager」。→ 若 Nuxt 特有踩坑有新增，歸 `~/.claude/learnings/`，不建 skill。

### ⚪ 無候選 — crm-projection

- Session 3 是**執行既有 `crm-projection` skill**，非新模式。無候選。

---

## 領域缺口追蹤（非本次抽 skill，供未來 harvest 對照）

- **製造運營 / 半導體業務敘事**：CLAUDE.md「Text Report Generation → D. 製造運營」標記多數 fallback 到通用流程；Session 1 是第一個 IC-YMS 業務案。累積到 2+ 案再抽 `ic-yms-solution-narrative`。

---

_Harvest 方法：比對 145+ 現有 skills，只提報不重複者。本批 session 成本顯示 $0.00、digest 較精簡，領域知識取樣有限，候選信心相應保守。_
