# Avatar VRM models

- `male.vrm` / `female.vrm` — 佔位模型，取自 github.com/madjin/vrm-samples
  （vroid/masc_vroid.vrm、fem_vroid.vrm；VRoid Studio 產出樣本）。
- .vrm 檔不進 git（.gitignore）；換機用上面 curl 重抓，或換成自製/購入模型
  （檔名不變即可，data/persona.conf 引用 /avatar/models/{male,female}.vrm）。
