# SD 文件模板

Copy 成 `docs/design/YYYY-MM-DD-<feature>-sd.md` 後逐節填。
**不要重新發明章節順序** —— `gstack-plan-eng-review` 與 `qa-dataflow` 都預期這個形狀。

Light 檔只填 §1 §3 §4 §8，其餘節寫「Light 檔略」並註明判準。

---

```markdown
---
feature: <feature-name>
date: YYYY-MM-DD
scale: full | light          # Step 0 檔位
triggers: [schema-change, cross-module, new-store]   # 命中哪幾個 Full 判準
requirement: docs/requirements/<feature>.md | 無
mockup: mockups/<feature>.html | 無 UI
status: draft | reviewed | superseded
---

# <Feature> 系統設計

> 一句話：<這次改動的一句話摘要>
> 依 YYYY-MM-DD 程式碼實查。

## §1 Scope

**對映需求**：US-1, US-3（或：bug root cause — <一句話>）

| In Scope | Out of Scope |
|----------|-------------|
| | |

## §2 資料模型

### 現況（實查）

| 表 / 實體 | 出處 | 關鍵欄位 | 目前誰寫 / 誰讀 |
|---|---|---|---|
| | `models.py:42` / `.schema orders` | | |

### Delta

| 動作 | 表.欄位 | 型別 | Null | 預設 | 索引 | 誰寫 | 誰讀 |
|------|---------|------|------|------|------|------|------|
| `+` | | | | | | | |
| `~` | | 舊 → 新 | | | | | |
| `-` | | | | | | | |

**遷移策略**：backfill 方式 / 舊資料處理 / rollback 可行性

**ER 圖**：`diagrams/er-<feature>-YYYY-MM.html` — 回答：<這張圖回答什麼問題>

## §3 模組拆分與職責邊界

| 模組 | 負責 | **不負責** | 依賴 |
|------|------|-----------|------|
| | | | |

**元件圖**：`diagrams/components-<feature>-YYYY-MM.html` — 回答：<...>

## §4 介面契約

### `<METHOD> /path` — <一句話用途>

- **前置條件**：需要 `<id>` 才能呼叫（來源：<哪個上游功能>）
- **輸入**：`{ field: type, required }`
- **成功輸出**：`{ ... }`
- **錯誤形狀**：

  | 情況 | HTTP | body | 呼叫端怎麼分辨 |
  |------|------|------|--------------|
  | | | | |

- **冪等性**：重送會怎樣

（每個介面重複一段）

## §5 關鍵流程

**只畫會出錯的那條。** 單一同步呼叫寫三行文字即可，不硬畫圖。

**序列圖**：`diagrams/sequence-<flow>-YYYY-MM.html` — 回答：<...>
錯誤分支用 `alt` 放同一張，不另開。

**狀態機**（有生命週期的實體才畫）：

| 從 | 到 | 誰觸發 | 條件 |
|----|----|--------|------|
| | | | |

## §6 功能關係圖（target）

照 `skills/qa/qa-dataflow/references/diagram-spec.md` 規格。
檔名與 actual 對齊：`diagrams/dataflow-target-YYYY-MM.html`

- 節點 = 功能（用**使用者看得到的名字**，內部元件名放副標）
- 邊 = **傳遞的識別碼**（每條都要標，只有箭頭 = 沒做）
- 必畫**回頭路**（退回 / 重做 / 重訓）與**旁掛**（唯讀工具，虛線）
- 每個帶的入口標「帶進來的是什麼」**與「不是什麼」**

| 帶 | 帶進來的是 | **不隨行的是** | 交出去的是 |
|----|-----------|--------------|-----------|
| | | | |

## §7 NFR 與反證關卡

| 項目 | 值 | 依據 |
|------|-----|------|
| 預期資料筆數 | | |
| 併發使用者 | | |
| 單次請求資料量 | | |
| 外部依賴 | | 掛掉會怎樣 |

**權限**：誰讀 / 誰寫 / 隔離在哪一層

### 反證關卡（teeth）

三態，`✗` 與 `◐` 後面必須接一句為什麼。**留至少一個 `✓` 當對照組。**

| 關卡 | 設計上擋什麼 | 狀態 | 擋不住什麼 / 怎麼被跨過去 |
|------|------------|------|------------------------|
| | | ✓ / ✗ / ◐ | |

## §8 未決事項與假設

| # | 問題 | 目前假設 | 誰能拍板 | 何時必須決定 |
|---|------|---------|---------|-------------|
| 1 | | | | |

「待補」是合法答案，編造不是。此節為空通常代表沒認真想邊界。

---

## 圖檔交付收據

每張圖一列，三種宣稱不准互相冒充。

| 圖 | mechanical_check | screenshot | visual_review | rounds |
|----|-----------------|-----------|--------------|--------|
| er-... | 0 errors, 2 warnings @1600×900 | /tmp/x.png | passed | 1 |
```
