# 稽核產出怎麼組織，才不會三個月後沒人看

Phase 4 的細節。一次稽核會生出七八個檔案，散著放等於沒交。

## 主表是主表，其他都是它某一格點進去的細節

一次稽核會生出七八個檔案，沒有組織的話三個月後沒人知道該看哪一份。用中心輻射的結構，
並在 `INDEX.md` 第一句就講明白：**功能關係圖是主表，其他所有東西都是它某一格點進去的細節。**

檔名要一眼看得出掛在主表哪裡：

```
dataflow-functions-<YYYY-MM>          ← 主表
node-<帶>-<節點>-<YYYY-MM>            ← 某一格的細節（圖或稽核）
dataflow-<視角>-<YYYY-MM>             ← 橫切視角（實體流轉、儲存層、模組全景）
```

「節點細節」與「橫切視角」要分開列：前者是放大主表的一格，後者是換一個角度看整張圖。

**節點細節不要另立山頭。** 同一格的新發現就更新同一份 `node-*`，不要每次分析都生一張新圖 ——
實際踩過：同一個議題一天內產出三張平行的圖，最後還要花時間收斂成兩張。

## 主表不只是稽核產出，是規劃基準

這是它最大的價值，也最容易被浪費 —— 交完就沒人再打開。寫進 `INDEX.md` 的維護規則：

- **要開發新功能時，先改主表**：在對應的帶上加一格、把線接起來、標上狀態，再開始寫程式。
  「這個功能的前置是什麼、它交出去什麼」在動工前就會被逼著想清楚。
- **狀態顏色要跟著實作走**：補完缺口就把紅框改實線，發現文件與實作不符就標黃框。
  **顏色過期比沒有顏色更糟** —— 沒顏色只是沒資訊，過期的顏色是錯誤資訊。
- **每張圖頁尾寫清楚依據哪一天的程式碼實查**，讀的人才知道還能不能信。
- **`INDEX.md` 結尾維護一節「現在主表上還亮著的缺口」**，一格一列。
  這是稽核報告與待辦清單的接縫 —— 缺口補完就從這裡劃掉，沒補完就一直亮著。

狀態顏色四種（比三種好用，「規劃中」讓還沒動工的設計也能上圖）：
實作與文件一致 · 文件說沒做但其實有 · 真的還沒做 · 規劃中。
