# <範圍> 資料流稽核（YYYY-MM-DD）

<!--
用法：整份複製，把 <> 換掉，用不到的節刪掉（不要留空標題）。
這份是給「接手的人」看的證據盤點，不是計劃 —— 目的是讓他不用重查一遍。
-->

**結論先講**：<一句話講最重要的落差。例：「鎖定 PG = system of record，實作停在分期 2 —— PG 在每一條路徑上都在檔案的下游，而且掛掉不擋出貨。」>

---

## 宣稱的流程

來源：`<檔案路徑 / ADR 編號 / memory key / 使用者口述>`（<日期>）

- <宣稱 1>
- <宣稱 2>

<!-- 沒有書面宣稱時，寫「無書面 target，以下 target 來自 <誰> <日期> 的口述」 -->

## 實際的流程（<YYYY-MM-DD> 程式碼實查）

| 宣稱的 | 實際的 | 證據 |
|---|---|---|
| <宣稱> | <實際> | `<file>:<line>` |
| <宣稱> | <實際> | `<file>:<line-range>` |

<!-- 每一列都要有 file:line。沒有行號的觀察不要放進這張表，放進「待查」。 -->

### 好消息

<已經對的部分。稽核報告只寫壞的話，接手的人會以為要全部重來。>

## 反證結果

### 拔依賴

| store／服務 | 預測會壞的出口 | 實際 | 判讀 |
|---|---|---|---|
| <name> | <出口> | <結果> | ✅ 邊是真的 / ❌ 邊是假的 / ⚠️ 隱性依賴 |

### 旗標

| 名稱 | 預設值 | 預設走哪條路 | 另一條有測試嗎 |
|---|---|---|---|
| `<FLAG>` | `<value>` | <path> | <yes/no> |

### 壞資料閘門

| 輸入 | 行為 | 判讀 |
|---|---|---|
| 空檔 | <行為> | ✅ / ⚠️ / ❌ |
| 半截檔 | <行為> | |
| 版本不認得 | <行為> | |


### 治理欄位有沒有牙齒

<!-- 一定要放對照組：把真的會擋請求的欄位跟純標籤排在同一張表，
     否則「沒牙齒」會被讀成挑剔，也看不出這是能力問題還是當初就沒打算擋。 -->

| 欄位 | 寫入 | 規則 | 稽核 | 有人讀它做決策嗎 | 判定 |
|---|---|---|---|---|---|
| `<真的有擋的欄位>` | ✅ | ✅ | ✅ | ✅ `<那一行條件式／查詢條件>` | 🟢 有牙齒 |
| `<只約束同類資料的>` | ✅ | ✅ | ✅ | ⚠️ `<只被拿來統計／顯示>` | 🟡 只咬自己 |
| `<純標籤>` | ✅ | ✅ | ✅ | ❌ 零讀取端 | 🔴 純標籤 |

具體缺口：<例如「retired 的版本照樣能推論」「金鑰不分環境」「不會降級舊的 PROD」>

### 被吞掉的失敗

| 位置 | 吞掉什麼 | 使用者看得到嗎 |
|---|---|---|
| `<file>:<line>` | <失敗> | <no> |

## 建議釘住的接縫

| # | 接縫 | 測試形狀 | 為什麼是這條 |
|---|---|---|---|
| 1 | <seam> | tracer bullet / parity / 外部真值 / 筆數守恆 | <壞掉會沒人發現的理由> |

## 不要動壞的東西

<!-- 這節最保護人。稽核報告會被當成「這裡全爛了」的授權，接手的人一刀砍掉還在守著的東西。 -->

- **<現有的防護>** —— <守著什麼>（`<測試檔或機制>`）
- **<刻意的設計>** —— <為什麼刻意>，不要為了 <改動> 把它接起來

## 接手要先決定的事

<!-- 產品／架構決策，不要替使用者決定。每條寫清楚「為什麼非決定不可」。 -->

1. **<決策>。** <為什麼卡在這、選項有哪些、硬約束是什麼>
2. **<決策>。**

## 相關

- 實際資料流圖：`docs/architecture/dataflow-actual-<YYYY-MM>.html`
- 宣稱資料流圖：`docs/architecture/dataflow-target-<YYYY-MM>.html`
- <決策全文 / ADR / memory key>
